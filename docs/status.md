# 現状（最終更新: 2026-09-10 / v4.87.0）

セッションを始めたら、まずこのファイルを読む。
現行仕様は `docs/spec/`（**本体を読む前に `docs/spec/code_map.md`**）。
設計判断の理由は `docs/adr/`、軽い判断・地雷は `docs/decisions.md`。
**未来のタスクは GitHub Issues**、進め方は `docs/workflow.md`。
過去の経緯は `docs/archive/handoff_v4.md`（原本・更新しない）にある。

## いまの状態

- **公開URL**: https://dfcfr909-bit.github.io/foehn/ （`index.html` が `sotoki_v4.html` にリダイレクト）
  - ⚠ **リポジトリを `SotoKi` から `foehn` へ作り直した（ADR-0009）。**
    履歴の SHA・Issue番号・公開URLがすべて変わっている。
    **旧 `SotoKi` のクローンは使えない**（全SHAが別物）。取り直すこと
  - **Pages は Actions 方式で配信している**（`.github/workflows/pages.yml`）。
    設定画面で「Deploy from a branch」が選べなかったため → `docs/decisions.md`
    `main` にマージすると自動でデプロイされる
- **本体**: `sotoki_v4.html` 単一ファイル（**7,685行 / 関数約280**）。バンドラなし、uPlotは `vendor/` に同梱
  - ⚠ **ファイル名は変えない。** `sw.js`・`index.html`・`manifest`・テスト・`code_map.md` が
    この名前に依存している（55箇所）。リネームするなら独立したPRで
- **アプリ名は「ナギナビ」/ `NAGI NAV`。** 変えたのはリポジトリ名だけ
- **開発ブランチ**: `origin/main` から毎回新しく切る
  （`git checkout -B <branch> origin/main`）。直近は PR #25 / #27 / #28 / #29
- **マージは CI が緑なら確認なしでよい。** ただし判定・配信・公開範囲に触れるものは必ず確認
  → `docs/workflow.md`「マージ（確認なしでよい範囲）」（2026-08-20 合意）
- **開発の足回り**（2026-08-20 に一巡した）
  - デプロイ後、**配信物が一致するか自動で照合される**（`公開URLの検証` の `deliver`）。
    「直したのに変わらない」は、まずここのログを見る
  - **タグは Actions「タグを打つ」から打つ。** 手元の `git tag` は届かない（403）
  - **Nodeの版は `.nvmrc`、Chromiumの用意は `.github/actions/chromium`、
    スモークは `test.yml` に一本化。** 書き写すと `smoke_workflows` が落ちる
    → `docs/workflow.md`「CI の決まり」

## 進行中

- **ABC評価の風速を「山頂高度の気圧面風」に変えた（ADR-0006）。** 判定が甘すぎた不具合。
  地上10m風で判定していたため、モデル地形より高く突き出た山ほど風が弱く出ていた。
  皇海山(2,144m)はモデル地形1,405mで、10m風2.73m/s → 800hPa 21.02m/s。**終日A → C** に是正。
  - `judgePoint` / `THRESH` は**一切変更していない**。変えたのは入力データの質だけ
  - ⚠ **ランキングの見え方が一変する。** A の山域の多くが B/C になる。これが正しい姿。
    「厳しくなった」と言われても**閾値を戻さないこと**（ADR-0005と同じ罠）
  - 実機確認は #16。**座標ズレで判定が甘くなる**ことが分かったので、
    峰と分かっている選択では `areas.json` の標高を優先する（DEM頼みにしない）
  - ⚠⚠ **安全弁は撤回した（ADR-0011 / PR #56）。** ADR-0006 は「山頂がモデル地形より
    100m以上高いときだけ気圧面へ」という弁を置いていたが、**Open-Meteo が返す
    `elevation` の意味が変わり**、標高の数字だけが山頂に寄って風は地上10m風のまま、
    という状態になった。**110峰中109峰が黙って地上10m風へ落ちていた**
    （2026-09-06 の至仏山で利用者から発覚）。いまは**山頂の標高だけ**を見る
- 地図の山域・百名山レイヤー（#69）は実装済み。見た目の実機確認は #15
- 予報山域は **51山域/110峰**（日本百名山100座）。
  **座標は全110峰を国土地理院で検証済み（#13・PR #25）。** `note` の但し書きも外した
- **地図の山名検索を作り直した（#25 / #27 / #28）。** 情報源は **OSM＋国土地理院の2つ**。
  表記揺れ（`ヶ/ケ/ガ`・`ノ/の`）を作って引き、漢字の揺れ（`龍/竜` 等）は照合で畳む。
  結果は**近い順**に並べ、**標高**と**百名山の印**を添える。「高い順」トグルつき
  - ⚠ **地理院の地名検索はブラウザから叩けることを実機で確認済み**（2026-08-20）。
    CORSが通るか未検証のまま入れたが、候補に「（国土地理院）」が出た。
    落ちても黙って捨ててOSMだけに戻る作りは**そのまま残す**（相手方の都合で変わりうる）
- **ドキュメント構成の移行は完了した（段階6まで）。** `docs/project_structure_proposal.md` は役目を終えた
  - `.claude/` にスラッシュコマンド（`/test` `/status` `/spec` `/release`）と permissions を置いた
  - ラベル `feature` / `chore` / `needs-decision` は作成済み（`bug` は既定のもの）。
    **#1〜#16 への付与も完了**（`needs-decision` … #9 #10 #12 ／ `feature` … #11 ／ `chore` … 残り）
- **Netlify は撤去した（ADR-0008）。** プロジェクトを削除し `netlify.toml` も消した。
  PRのチェックは GitHub Actions のスモークテスト（`.github/workflows/test.yml`）に置き換え
  - ⚠ **マージ前に実機で触る手段は無くなった。** 実機確認はマージ後に公開URLで行う

## 未完・要確認

**Issueへ移した。** 詳細は各Issueにある（確認する場所・判定の分かれ目・逃げ道つき）。
ここには一覧だけ置く。

| # | 項目 | 種別 |
|---|---|---|
| [#1](https://github.com/dfcfr909-bit/foehn/issues/1) | ひまわりカラーの虫食い配信（保留中） | 実機確認 |
| [#2](https://github.com/dfcfr909-bit/foehn/issues/2) | 雲の着色（`SAT_TINTS`）が効いているか | 実機確認 |
| [#3](https://github.com/dfcfr909-bit/foehn/issues/3) | 雨の予告の分きざみ（気象庁タイルのCORS） | 実機確認 |
| [#4](https://github.com/dfcfr909-bit/foehn/issues/4) | 地図レイヤーが表示されるか（火山土地条件図のID等） | 実機確認 |
| [#5](https://github.com/dfcfr909-bit/foehn/issues/5) | jma_seamlessの実予報期間 | 実機確認 |
| [#6](https://github.com/dfcfr909-bit/foehn/issues/6) | 気圧面ごとの雲量・3層フォールバックの見た目 | 実機確認 |
| [#7](https://github.com/dfcfr909-bit/foehn/issues/7) | JMA bosai のCORS（#8が先） | 実機確認 |
| [#8](https://github.com/dfcfr909-bit/foehn/issues/8) | `buildSpots.mjs` 未実行（`data/spots.json` が無い） | タスク |
| [#9](https://github.com/dfcfr909-bit/foehn/issues/9) | AI全国概況が非表示（`GLM_API_KEY` 待ち） | 要判断 |
| [#10](https://github.com/dfcfr909-bit/foehn/issues/10) | CS立体図の配信範囲（URL待ち） | 要判断 |
| [#11](https://github.com/dfcfr909-bit/foehn/issues/11) | スクラバー帯のなぞり心地 | タスク |
| [#12](https://github.com/dfcfr909-bit/foehn/issues/12) | ライセンス未設定 | 要判断 |
| ~~[#13](https://github.com/dfcfr909-bit/foehn/issues/13)~~ | ~~`areas.json` の座標を検証~~ **完了（PR #25）** | 済 |
| [#14](https://github.com/dfcfr909-bit/foehn/issues/14) | ランキング110地点がOpen-Meteoのレートに収まるか | 実機確認 |
| [#15](https://github.com/dfcfr909-bit/foehn/issues/15) | 山域レイヤーの見た目（札の重なり・△の見え方） | 実機確認 |
| [#16](https://github.com/dfcfr909-bit/foehn/issues/16) | 山頂高度の風による判定が実際の予報と合うか（ADR-0006） | 実機確認 |
| [#26](https://github.com/dfcfr909-bit/foehn/issues/26) | 気圧配置 → **天気図（PR #44）と信頼度（PR #54）で代替。計算した等圧線は撤去（ADR-0010）** | 要判断 |

> **この表は増やさない。** 新しい未確認が出たら Issue を立てて1行足す。
> 運用は `docs/workflow.md`。

## 直近の変更（3件まで。古いものは消す）

- **判定の原因をグラフの上に示し始めた（PR #62 / #63）。**
  「下の赤黄緑の帯を無くしたい。判定の元になっている項目のグラフを赤と黄で塗れば」
  という提案から。降水・降雪のバーの**縁**を判定の色にした（B=橙 / C=赤）。
  - ⚠ **風は以前から塗りで示していた。** 気圧は色が付くが**判定とは別の閾値**（未整理）。
    **体感温度は画面に描かれていない**——ここが済むまで帯は消せない
  - ⚠ **Aには何も付けない。** どの時間にも出ると狼少年になる（`smoke_mark` が見張る）
  - ⚠ **縁は「コの字」（下の辺を引かない）。** 閉じるとバーの足元の地表帯と重なり、
    印ではなく「地面の線」に見える（実機で指摘）
  - ⚠⚠ **赤（C）は橙（B）より後に描く。** 隣り合う縁は重なるので、
    1周だと時刻が後ろの棒が勝ち、**赤の上に橙が乗る**（実機で指摘）
  - あわせて**地点の標高の札**を固定ガターへ（#62）。⚠ 前はチャート本体の左端に
    描いていて**事実上ずっと画面の外**だった
- **選択情報の窓を削り、虫眼鏡を止め、凡例の数字の位置を直した（PR #60）。**
  - 窓は **239×174 → 133×168px**（チャート幅の61%→34%）。見出しを1文字に、
    突風を風の行に畳み、風の高度を最下段の但し書きへ出した
  - ⚠ **削ってはいけないもの**——判定に使った風の高度（ADR-0006/0011）、
    体感温度と気圧の変化（判定に使う）、風と雨の言葉。`smoke_popup` が見張る
  - iOSの虫眼鏡は `#charts-outer` と `#scrubber` で選択を切って止めた。
    ⚠ **`body` へ一括指定しない**（地図の緯度経度はコピーさせたい）
  - ⚠ 凡例の値を右寄せにしていたので、桁が短いと数字がドットから離れていた。左寄せへ
- **凡例に選択時刻の値を出した（PR #58）。** 「スライダーで操作した際、この窓で
  グラフの表示が見えない部分が多い」という指摘への**案1**。
  ⚠ ポップアップは指の近くに出るので、なぞるほど読みたいパネルを覆う。
  指が来ない場所（グラフの下の凡例）に 気温・降水・降雪・風速・突風 を置いた。
  - ⚠ **幅は数えてから決めた。** 名前＋値では **515px 必要・366px しか無い**（390px端末）。
    名前を落として色ドット＋値にした。**名前と単位は既に軸が持っている**
  - ⚠ **桁数が変わっても項目が動かないこと**が肝（`min-width`＋`tabular-nums`）。
    横に踊ると元の「窓が邪魔」より読みにくくなる
  - **案2（窓をタップ時だけに）は未着手。** 実機で凡例の効き具合を見てから決める

## 次セッションの最初のプロンプト

⚠ このリポジトリは `SotoKi` から作り直した `foehn`（ADR-0009）。
手元に旧 `SotoKi` のクローンがあっても**使わずに取り直すこと**（全SHAが別物）。

**#13（座標検証）・開発の足回り（#31〜#36）・Issue #26（気圧配置まわり）はすべて一段落。**
`v4.86.0` を打ったところ（PR #66 は下書きのまま）。

⚠ **長押しの虫眼鏡と「グラフが白く消える」は解決した**（`v4.82.0` / PR #60）。
どちらも原因は iOS のテキスト選択UIだった。利用者に実機で確認済み。

⚠⚠ **いちばん先にやること: 判定の原因をグラフに示す（続き）。**
「下の赤黄緑の帯を無くしたい」という提案の途中。降水（PR #63）と体感温度は済み。残りはこの順。

| 段階 | 中身 |
|---|---|
| **A** | ✅ **済（v4.87.0 / PR #66）。** 気温パネルの下端 11px を体感温度の色で塗り、判定が B/C の時間を枠で囲む。⚠ **色の目盛りは固定**（日ごとに伸縮させない）。⚠ `FEEL_BAND_H` は uPlot の `padding` とガターの `skyBottom` の**2か所と対**。⚠ 判定は**寒い側だけ**（暑い側は色で伝わるが枠は付かない） |
| **B** | 気圧の色と判定の閾値の食い違い。\|ΔP6h\|≧3 から警告色が付くが、**判定のBは6・Cは10**——「画面は警告色なのに判定はA」。⚠ **2026-09-09 に「いまは直さない」と決めた**（体感温度が先）。直すときも動かすのは**見た目だけ** |
| **C** | A・Bが済んでから、帯を消すかを実機で判断する |

### 体感温度の帯（PR #66・v4.87.0）

気温パネルの下端 11px を体感温度の色で塗り、判定が B/C の時間を枠で囲む。

⚠⚠ **濃さは寒い側に寄せてある。** 最初の目盛り（10℃中立・上下均等）では
**体感26〜31℃のふつうの夏日で帯が終日べったり橙**になり、判定はAのままだった——
`GRADE_COL.B` と紛らわしく「Aなのに警告色」に見えた（実機の画で確認 → 2026-09-10 利用者判断で修正）。
**8〜33℃ は淡い無彩色**、濃さの大半は寒い側（-3 → -15 → -25℃）へ。

- ⚠ 寒い側の変化点は `THRESH.apparentA` / `apparentB` と**同じ数値**。
  片方だけ変えると `smoke_feel.mjs` が落ちる
- ⚠ **暑い側の数値（33 / 38 / 45 / 50℃）は暫定。** 根拠は下記の宿題を参照
- ⚠ 枠（B/C）の出方は**冬にならないと実機で見られない**（2026-09-10 時点で未確認）

### 説明ページ（`about.html` / 2026-09-10）

「友人と共有したい。判断基準・データの出どころ・経緯を説明できるページが欲しい」から作った。
アプリの**版数表示がここへの入口**。仕様は `docs/spec/ui.md`。

- ⚠⚠ **閾値を書き写している。** `THRESH` を触ったらこのページも直す
  （`smoke_about.mjs` が行ごとに突き合わせて落とす）
- ⚠ **未対応**: `sw.js` の `PRECACHE` に `./about.html` が無いため、
  **オフラインで開くとアプリ本体が返る**。`sw.js` は「必ず確認」対象なので触っていない
- ⚠ Issue #12（ライセンス未設定）に触れてある。**再利用・再配布の条件は未定**のまま

### ⚠⚠ 宿題: 暑い側の判定が無い（熱中症の警告を出していない）

**2026-09-10 に利用者が気づいた**——「夏はむしろ暑いのが問題になるのに
熱中症アラートを作ってませんでしたね」。いまの判定（`abcScoreInv`）は**寒い側だけ**を見る。

調べた基準（環境省・日本生気象学会）:

| 区分 | 暑さ指数 WBGT |
|---|---|
| 熱中症**特別**警戒アラート | 35以上（都道府県内の全地点で予測） |
| 熱中症警戒アラート | **33以上**（府県予報区内のいずれかの地点で予測） |
| 危険 | 31以上 |
| 厳重警戒 | 28〜31（2025年6月から職場の環境管理義務ライン） |
| 警戒 | 25〜28 / 注意 21〜25 / ほぼ安全 21未満 |

⚠⚠ **WBGT は気温ではない。** 湿球温度が7割を占め、湿度・輻射熱を織り込む。
**Open-Meteo の `apparent_temperature` とも別物**で、公式の換算は無い
（日射込みで気温よりかなり高く出る。実機で気温25℃→体感31℃）。
目安は湿度60%で 気温31℃ ≒ WBGT 28、気温35℃ ≒ WBGT 32。
暑さ指数の式で当てると厳重警戒が体感35℃前後、危険が体感45℃前後——
**帯の暑い側の数値はこの線で置いた暫定値**。

やるなら2段階。**どちらも未着手・未承認。**

1. **WBGT を計算して持つ。** Open-Meteo は湿度・日射を返すので近似できる。
   ⚠ 体感温度で代用しない（別物）
2. **判定に暑い側を足す。** ⚠⚠ `THRESH` に暑い側の閾値が無いので、
   **`abcScoreInv` を足すだけでは済まない可能性がある**——ABC評価ロジックは変更禁止
   （`CLAUDE.md`）。着手前に必ず確認すること

⚠ 山の上は平地より涼しいが、**低山の夏や樹林帯では効く**。無視してよい話ではない。

> docs/status.md を読んだうえで、**暑い側の判定（熱中症）をどうするか**から進めて。
> ⚠ WBGT は体感温度とは別物。⚠ ABC評価ロジックに触れるので着手前に確認すること。
> `origin/main` から新しいブランチを切ること。

⚠ **案2（ポップアップをタップ時だけ出す）は保留のまま。** 窓は PR #60 で
239×174 → 133×168px に縮めたので、**まず実機で足りているかを見てから**決める。

⚠ **実機で至仏山も見てもらうこと（#16）。** ADR-0011 で判定に使う風が変わった。
ポップアップの風速の下が `@2228m` になっていれば効いている
（`@地上10m（山頂2228m）` ならキャッシュが残っている）。
⚠ **穏やかな日はどちらもAなので、判定の字ではなくこの小さい表示で見る。**

そのあとは **山域の分け方の見直し**。

> `富士周辺` / `石鎚・剣山`(91km) / `九重・祖母`(30km) の3つ（下の申し送り）。
> **座標が全部直ったので、距離の再計算からやり直すこと**（以前の km は古い座標のもの）。
> ⚠ 円の中心の計算式（重心）は**変えない**と決めてある → `docs/decisions.md` 2026-08-09。
> 直すのは**データ側**（分け方そのもの）

⚠ **どちらも ABC評価ロジック（`abcScore` / `abcScoreInv` / `judgePoint`）には触らない。**
制約: 段階ごとにcommitを分割すること。マージの可否は `docs/workflow.md` の線引きに従う。

## 未処理の申し送り

- ⚠ **`smoke_mapui` の「古いレイヤーは残さない（重ならない）」がCIで稀に落ちる（フレーク）。**
  **まず再実行すること。** 他のテストが道連れで落ちていないか（1件だけの失敗か）を確認材料にする。
  - **2026-08-13 に3回踏んだ**（#19 のCI／PR #20 で手元／PR #21 のCIで2回連続）。
    いずれも1件だけ・値は常に「3」・所要は約105秒。3回目の再実行で通った。
    **変更内容とは無関係**（コードを1行も変えていないブランチでも出た）
  - ⚠ **原因はまだ分かっていない。** 「遅いから」という見立ては**外れの可能性が高い**。
    手元で実測すると2枚になるまで **34ms**（当時の上限12,000ms）で、待ちは制約になっていなかった
  - **次に落ちたらログで切り分けられる**ようにした（PR #22）。残存レイヤーのURL・
    `staleWxLayers` の中身・ONのオーバーレイ・実際の待ち時間が出る。
    3枚目が「外れ損ねた古いレイヤー」か「別種のレイヤー」かはそれで分かる。
    **落ちたログを捨てないこと**（再実行で流れる前に控える）
- ⚠ **外部APIが返す数字の「意味」が変わったことに、半月以上気づけなかった**（ADR-0011）。
  Open-Meteo の `elevation` は 2026-08 に皇海山で 1,405m、2026-09 に 2,127m を返した。
  エラーも空白も出ず、**判定が甘くなるだけ**だったので、利用者の報告でしか分からなかった。
  - **他にも同じ形の依存が無いか、まだ洗い出していない。**
    外部の数字で分岐している箇所を一度数えること
  - `scripts/probeWind.mjs` は毎回 `elevation` の効き目を確かめ直すが、
    これは**風だけ**。気温・雲量などは見ていない
  - ⚠ **Claude のセッションからはタグを打てない。** push が 403 で拒否される
    （ブランチは push できるのでタグ ref だけの制限）。**人間が打つ**
  - スマホからは **Releases → New release → Create new tag → Publish release**。
    ⚠ `Save draft` ではタグは作られない（`on publish` と書いてあるとおり）。
    Actions の手動実行で打てるようにする案もある（「山頂座標の検査」と同じ形）
- ⚠ **旧URLの PWA が端末に残っている場合は入れ直す。** 旧リポジトリ `SotoKi` は
  2026-08-14 に削除した（ADR-0009 の対応はこれで完了。`refs/pull/*` の残骸も消えた）。
  旧URLは404になるので、ホーム画面のアイコンは消して新URLから入れ直すこと
- **笙ヶ岳は機械で解けず、地図の長押しで決めた（#13・解決済み。手順の実例として残す）。**
  `snapPeaks` は半径6kmに `1,635m±15m` の局所最高点を見つけられなかった。
  一致窓を30mに広げると 39.0971,140.0042（1,664m・**Δ+29m**）を掴むが、これは
  三角点から0.52km離れた**別の高まり**で、採ってはいけないものだった。
  正解は 39.0927,140.0020（DEM 1,633m・**Δ−2m**）で、地図で △1634.9 を
  長押しして読んだ。**Δの符号が決め手**——DEMは山頂を数m低く読むので、
  Δが正に振れた案は山頂ではない。
  ⚠ **道具が答えを出せないことがある。** そのとき推測で埋めない（2026-08-10 の項）
  - ⚠ **後日判明: 地名検索で引けなかったのは表記揺れが原因だった。**
    地理院は「笙ガ岳」で登録しており、「笙ヶ岳」では当たらなかった。
    「辞書に無い」と結論づけたのは誤り。いまは揺れを吸収する（2026-08-14 の項）
- **地理院の「基準点成果等閲覧サービス」は未調査。** 三角点の成果表があれば座標の
  根拠として最も明確だが、機械可読の窓口があるか確かめていない。
  ⚠ **三角点は山頂と一致しないことが多い**（富士山の二等三角点と剣ヶ峰）。
  使うとしても「山の同定」までで、最高地点の特定はDEMに任せる
- **二百・三百名山**は保留。選定に揺れがあり同名峰の同定も要るため、まず百名山だけで作る。
  広げるなら `hyakumeizan` と同じ形で `nihyaku` / `sanbyaku` を足す（座標調達は #8/#13 と同根）
- **山域の分け方に見直したい所がある**（円の中心が主峰からズレる形で表面化した）。
  計算式ではなく**データ側**の問題なので、#13 の座標検証と一緒に見るとよい。
  - `富士周辺` … 富士山＋三ツ峠山。**富士山は独立峰なので1座だけの山域が素直**
    （三ツ峠山は百名山でなく山塊も別。いま中心が11km北へ寄っている）
  - `石鎚・剣山` … 2峰で**91km**。粒度の方針（`docs/spec/judge.md`）から外れている
  - `九重・祖母` … 30km。上2つほどではないが同種
  - ⚠ 円の中心の計算式（重心）は**変えない**と決めた。理由は `docs/decisions.md` 2026-08-09
