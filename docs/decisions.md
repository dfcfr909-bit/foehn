# Decision Log

軽い判断・調整値・実装上の落とし穴を1行で残す（追記のみ。過去の行は書き換えない）。
**構造的で後戻りが高くつく判断は `docs/adr/` へ。**
判定基準は「半年後に『なんでこうなってるの？』と聞かれるか？」→ Yes なら ADR。

肥大したら年ごとに割る（`decisions.md` → `decisions/2026.md`）。

---

## 2026-08

- 2026-08-07 `docs/archive/handoff_v4.md`「重要な経緯」6〜9番・11〜14番を本ファイルへ移した（以下）。原本は更新しない参照用として残す。

### 移設分（版数は当該の修正が入った版）

- **時間移動UIは「下部スクラバー帯＋タップ」**（v4.30.0）。「チャート横スワイプ」→「スライダー＋タップ」→現行、と2度変えている。スライダーが使いにくかった原因は**密度**で、73時間を幅約336pxに載せて1時間≒4.6px、指1本が約9時間ぶんに相当していた。帯は1時間=14pxなので指1本が約3時間。**チャート直接スワイプは一度採用して撤回した方式なので、戻すときは慎重に**（現在チャートの横スワイプは `overflow-x: hidden` で無効）。
- **お気に入り円柱のDOMを作り直さない**（v4.30.0で修正）。地点を選ぶたびに `renderFavRotary()` が `innerHTML` を全消しして駒を作り直し→スクロール位置がリセット→再センタリング、の二段構えで引っかかっていた。いまは地点リストの顔ぶれが変わらない限り作り直さない（`favRotaryKey` で判定）。**ここに手を入れるときはDOM再構築を増やさないこと。**
- **選択線の位置を固定値に戻すと末尾の空白が戻る**（v4.31.0で修正）。選択線を画面の14%位置に固定していたため、最後の時刻を選択線へ持ってくるには右に画面幅の86%ぶんの余白が構造的に必要で、それが空白に見えていた。いまは `chartPadL`/`chartPadR`=0 とし、選択線はふだん `SCRUB_POS`(50%) に置いて両端の ramp px ぶんだけ端へ寄せることで、余白ゼロのまま先頭・末尾に届かせている。
- **狭い枠での円柱は「半径」でなく「1駒の角度」を広げる**（`FAV_ANGLE`）。ヘッダーへ移した際に隣の駒が正面の駒に重なった。角度を広げると隣の駒がより外側（x = R sinθ）へ行き、かつ強く圧縮される（幅 ∝ cosθ）ので重なりが解ける。駒は絶対配置なので、**枠外へ出たものは `display:none` にしないとスクロール範囲が狂う**。
- **スクロール追従をイベント頻度に依存させない**（上下の赤線のズレ。原因3つを段階的に潰した）。①選択線の更新を `requestAnimationFrame` に逃がして1フレーム遅れた ②なぞり終わりの吸い付きが smooth で帯の慣性と競合した（以上 v4.33.0） ③**本命**＝追従を scroll イベントに載せていた。帯はコンポジタ側で滑らかに動くのに対し scroll イベントは間引かれるため、JSで動かす側だけが遅れる（v4.34.0）。**なぞり中は rAF ループで毎フレーム実測し、上下の線は同じ一つの値で置く。吸い付きは即時。**
- **チャートと帯は必ず同時に動かす**（「現在」ボタンで飛ばない件。v4.36.0で修正）。チャートだけを `scrollTo({behavior:'smooth'})` で動かしていたため、ミラーで動いた帯のscrollイベントが「なぞり」と誤検出され、`scrubFrame` が `chartsWrapper.scrollLeft` を書いてアニメーションを打ち切り、元の index へ吸い付いていた（「下のバーは一瞬飛ぶが上が追従せず、下も元に戻る」症状）。**標準の smooth スクロールは片方しか動かせないので使わず、自前rAFの `animateScrollTo()` を使う。**
- **帯とチャートは同じ座標系（余白・px/h・スクロール量・`idxToX()`）を共有させる**（上下の時刻のズレ）。帯の指標を中央50%固定、チャートの選択線を画面の `SCRUB_POS`(14%) に置いていたため両者が別の場所を指し、目盛り間隔も帯14px/h対チャート約13.08px/hで違っていた。**どちらか一方だけの寸法をいじるとまたズレる。**
- **気象庁の `targetTimes` は「古い順」の配列。実況は末尾側から選ぶ**（衛星の雲が実況と大きくずれた件。v4.76.1で修正）。`jmaTimes` が `list.find(t => t.basetime === t.validtime)` で**先頭＝一番古い実況**を拾っていた。同ファイルの `nowcastSeries` は最初から一番新しい実況を選んでおり、**同じ時刻表を2通りに解釈していた**のが根本。両方 `latestObsTime` に寄せてある。テストのスタブが時刻表を**1件しか返していなかった**ため先頭を拾う実装でも通ってしまった → **時刻表を模すスタブは必ず複数件・古い順で返すこと。**
- **衛星の位置ズレで「投影」を再調査しないこと**（2026-08-07 クローズ）。修正前にオーバーレイの外枠がスクショで平行四辺形に歪んで見えたため投影も疑って記録していたが、v4.76.1 の実機確認で雲の位置・降雨レーダーとも実況と一致した。**ズレは時刻起因が全て。** 将来また位置ズレが出たら、まず疑うのは投影ではなく時刻表の選び方。

### 2026-08-09

- **地図の山域レイヤーは「一般的な山地・山脈図」にしない**（#69）。要望は書籍にあるような山脈の色分け図だったが、**そういう図を配信している公式タイルが無い**うえ、あっても**ランキングの山域とはズレて混乱する**。`areas.json` から**評価単位そのもの**を描く方式にしたので、地図と一覧が必ず一致し、判定の色（A/B/C）まで乗せられる。
- **山域の円の中心は「峰の単純な重心」のまま**（#69。標高加重と最高峰中心を却下）。`富士周辺` で富士山が円の南の縁に来る（中心とのズレ11km）のは、同居する三ツ峠山(1,785m)が重心を北へ引くため。検討した代案は2つとも割に合わない: **標高加重**は 11.0→7.1km と効果が小さいうえ全山域の半径が+2%、「標高が高い＝中心」という理屈も薄い。**最高峰中心**は半径合計が**+47%**に膨らみ、石鎚・剣山は 49.5→95km で四国が円で埋まる（半径は最遠の峰まで届く必要があるため、中心を端に寄せると必ず膨らむ）。**歪みの原因は計算式ではなくデータ側**——富士山は独立峰なので本来1座だけの山域が素直。直すなら `areas.json` の分け方を見直すこと。
- **山域レイヤーの色は「ランキングを開いた後」にだけ付ける**（#69）。地図を開いただけで全国110地点ぶんの気象データを引くと通信が重く、Open-Meteoのレート（#14）を悪化させる。`refreshRanking()` が組んだ**あと**に `areaGradeById` を渡す。色がまだ無いことは**パネルに出す**（黙って灰色にしない）。
- **`pickMapPoint` の二重定義に注意**（#69の作業中に発見・修正）。3引数版（名前つきで選ぶ）が、後から宣言された2引数版（長押しのピン）に**関数宣言の巻き上げで上書き**されていた。そのため検索結果をタップしても**名前が捨てられ逆ジオコーディングし直し、`mapFlyTo` も呼ばれず地図が動かなかった**。長押し側を `pickPinPoint` に分離。**同名の関数を後方で再宣言しないこと**（単一HTMLで全部が同じスコープなので起きやすい）。

### 2026-08-10

- **`areas.json` の座標は目視で検証すること。推測で直さない**（皇海山で2回外した）。
  「約490m南西」と見積もって直したら、DEMの読みが 1,390m → 1,247m と**悪化**した。
  実際のズレは**5.2km・ほぼ真南**で、桁が違っていた（正: 36.6899, 139.3370）。
  手順は2手——`node scripts/checkPeaks.mjs` でズレている峰を洗い出し、
  **地図で山頂を長押しして `#map-latlon` に出る座標を読む**。
  ⚠ 地図に緯度経度を出しているのはこのため。**消さないこと**。
  ⚠ DEMは約10mメッシュの標本なので、山頂の公称標高より数m低く出る
  （皇海山は 2,144m に対し 2,141m）。**それ以上下がらなければ山頂を捉えている。**
  `elev` の方を実測値に書き換えないこと（公称の山頂標高が正）。
- **GitHub Pages は Actions 方式で配信する**（`.github/workflows/pages.yml`）。移行後の `foehn` で
  Settings → Pages の Source が「GitHub Actions」しか選べず、手順書が指定した
  「Deploy from a branch（main / root）」を選択できなかったため。**配信されるファイルも公開URLも
  ブランチ配信と同じ**で、変わるのは配信の仕組みだけ。`configure-pages` の `enablement: true` で
  Pages の有効化自体もワークフロー側から行う（設定画面から有効にできなかったので、
  手作業を残さないため）。⚠ 直下をまるごと上げるので、置いたものは公開される。
- **`sw.js` はエラー応答をキャッシュしない**（`res.ok` を見てから `cache.put`）。
  ナビゲーション処理だけ見落としていて、404や500がHTMLキャッシュに残ると
  以後は圏外でもそのエラーが返っていた。**「圏外でも立ち上がる」が壊れる**ので
  静的ファイル側・タイル側と同じ扱いに揃えた。`CACHE_VERSION` は上げていない
  （配信ファイルの構成は変わらないため）。
  ⚠ 検査は**ナビゲーション処理の分岐だけを切り出して**行う（`tests/smoke_pwa.js` の
  `navBlock()`）。ファイル全体を見ると静的ファイル側の `res.ok` を拾ってしまい、
  バグがあっても通る。既存の `htmlNetworkFirst` がまさにそれで、この不具合を
  素通りさせていた。
- **`smoke_mapui` のフレークは「原因を出させる」ところから直す。** 落ちたときに
  枚数（`3`）しか出しておらず、**3枚目が何なのか分からないまま**再実行で通って
  しまうため、2026-08-13 に3回踏んでも原因に近づけなかった。
  残存レイヤーのURL・`staleWxLayers` の中身・ONのオーバーレイ・実際の待ち時間を
  出すようにした。⚠ **枚数だけを出す形に戻さないこと。**
  併せて待ちの上限を `WX_DROP_MS` から導くようにした（固定12秒だった）。
  逃げ道のタイマーは `await jmaTimes()` の**あと**に仕掛かるので、
  固定値だと逃げ道より短くなりうる。**これは予防であって、原因の特定ではない**
  （手元では2枚になるまで34ms。上限は制約になっていなかった）。
  ⚠ **落ちたログは再実行の前に控えること。** 再実行すると、せっかく出させた診断ごと
  流れて消える。2026-08-13 は3回踏んで**3回とも取り損ねた**（人もAIも、反射的に
  再実行してしまう）。フレークの手当ては `docs/status.md` にも書くが、あちらは
  毎セッション上書きするので**消えない置き場はここ**。順番は「控える → 再実行」。

### 2026-08-14

- **山頂の吸着は「elev に一致する最も近い点」では山頂を指さない**（#13）。
  一致窓は ±MATCH なので、山頂を芯とする円錐では **elev−MATCH の等高線**が
  いちばん外側に広がる。元の座標がずれていると、その等高線のうち手前側の点が
  「最も近い一致点」になり、**山頂の手前・約MATCH m低い肩で止まる**。
  止まる向きは常に元のズレの方向なので、誤差が誤差を呼ぶ。
  実測がそのまま証拠で、要確認12峰の**全部**が `elev−14〜15m` に着地していた
  （平ヶ岳 −15 / 笠ヶ岳 −14 / 常念岳 −14 …）。近傍最高点は公称標高とほぼ一致
  していた（笠ヶ岳2895/2898・西吾妻山2035/2035）ので、**山頂は0.5km以内に
  あったのに手前で止まっていた**。条件を2つにして直した——
  ① elev と一致する ② 周囲0.5kmに自分より高い点が無い（＝峰の芯）。
  ⚠ **同値は許す。** 平らな台地は周囲に自分より高い点が無いのでそれ自体が頂上で、
  弾くとメサ状の山頂を落とす。落とすのは「もっと高い芯を持つ輪」だけ。
  ⚠ Δ（DEMの読み − 公称標高）を出力に足した。**−14〜15m が並んだらこの偏りの再発**。
- **移動距離の小ささを「安全」の根拠にしない**（#13）。旧規則の修正案のうち
  「1km未満だから同一山塊内で安全」として15件を先に入れたが、規則を直して回し直すと
  **越後駒ヶ岳はさらに4.76km動いた**（37.0809 → 37.1236）。先に入れた値は
  旧規則が掴んだ山頂でない点だった。**小さく動いたものほど「もう合っている」ように
  見えるが、偏りは距離と無関係にかかる。**
- **標高だけでは「どの山か」を確かめられない**（#13）。DEMの吸着は同じ標高の別の峰に
  当たっても気づけない。実際 `areas.json` の平ヶ岳の座標は**至仏山の近く**を指しており、
  半径6kmの探索で出た「範囲の最高点2225m」は至仏山(2,228m)の読みだった。
  地理院の地名検索（`scripts/searchPeaks.mjs`）で名前から座標を引いて突き合わせる。
  **標高で「山頂かどうか」、名前で「どの山か」**という分担にする。
  ⚠ 地名検索は**座標の採用には使わない**。代表点は最高地点とは限らず、三角点が山頂と
  一致しない山も多い（富士山の二等三角点と剣ヶ峰）。`elev` は公称の山頂標高なので、
  代表点をそのまま入れると標高と座標が別の場所を指す。
  ⚠ **完全一致で絞ること。** 部分一致だと地名辞書に無い山で住所・施設を拾う
  （飯豊本山→「福島県小野町大字飯豊本飯豊」100km、日光白根山→「日光白根山ロープウェイ」）。
  16件の要確認のうち7件が誤検出で、**本物（大朝日岳13.25km・平ヶ岳6.64km・五竜岳6.41km）が
  その中に埋もれていた。**
- **Actions で回す道具は非TTYでも進捗を出すこと**（#13）。`snapPeaks.mjs` の進捗表示を
  `isTTY` 限定にしていたため、**29分間まったく出力が無かった**。ログはジョブ完了まで
  取れないので、外からは「重い」のか「1本のリクエストで止まった」のか区別できない。
  併せて `fetch` に `AbortSignal.timeout` と再試行を入れた（時限が無いと無言で止まる）。
- **タイルを際限なくキャッシュしない**（#13）。256×256を入れ子のJS配列で持ったまま
  全部残していたら、`--all`（110峰・約4,000枚）で**ヒープ4GBを使い切って落ちた**
  （90/110で OOM）。`Float32Array`（1枚256KB）に持ち替え、400枚で頭打ちにした。
  ⚠ データ無しは **-Infinity**。NaN にすると移動最大値フィルタの比較が常に false になり、
  単調デックから抜けなくなる。
- **ワークフローの選択肢に `on`/`off`・`yes`/`no` を使わない**。YAMLが真偽値として読み、
  許可値の一覧が空になって `workflow_dispatch` が422で弾かれる
  （`Provided value 'on' for input 'all' not in the list of allowed values []`）。
  `changed`/`all` のように真偽値でない語にする。
- **`checkPeaks` の許容差200mは緩すぎた**（#13）。笙ヶ岳（1,635m）は座標が山頂から
  1km以上外れていたのに、指していた斜面が1,518mあったため差は**117m**にとどまり、
  200mの網を抜けて「合格」になっていた。**「全110峰が200m以内」は、山頂を指している
  ことの保証にならない。** 全110峰を60mで洗い直すと要確認は笙ヶ岳のただ1件で、
  正しく山頂に載っている109峰はすべて60m以内だった。既定を**100m**に下げた
  （109峰の60mと笙ヶ岳の117mの間を取った）。
- **一致窓（`--match`）を広げて解決しようとしない**（#13）。笙ヶ岳が見つからないのは
  DEMが尖った山頂を低く読むせいだと考えて30mに広げたら、**Δ+29m の点**を掴んだ。
  DEMは山頂を数m低く読むのであって**高く読むことはない**。つまりそれは公称より
  29m高い別の高まりで、三角点ではなかった（移動0.40kmで、地図上の△1634.9には届いていない）。
  ⚠ **Δが正に振れた修正案は疑うこと。** 窓を広げるのは最後の手段で、広げたら
  必ず地図と突き合わせる。
- **`Δ` の符号が「山頂かどうか」の決め手になる**（#13・笙ヶ岳で確定）。DEMは10mメッシュの
  標本なので山頂を**数m低く**読む。逆に**公称より高く出たら、そこは目的の山頂ではない**。
  笙ヶ岳では一致窓を30mに広げて出た 39.0971,140.0042 が **Δ+29m** で、
  地図で読んだ三角点 39.0927,140.0020（**Δ−2m**）から0.52km離れた別の高まりだった。
  **Δが正の修正案は採らない。**
- **山名の表記揺れに対応する。1字違うだけで検索は空振りする**（#13 の最後の落とし穴）。
  鳥海山の笙ヶ岳は地理院に「笙**ガ**岳」、OSMに「笙**ケ**岳 二峰」で載っている。
  利用者が素直に「笙ヶ岳」と打つと**岐阜県の笙ヶ岳(908m)しか出ず、選ぶと500km先へ飛ぶ**。
  検証スクリプトも同じ理由で空振りし、**「地名辞書に無い＝同名の別の山」と誤って
  結論づけていた**（実際には有り、引けていなかっただけ）。
  - 揺れの組は `ヶ/ケ/ガ/が/ヵ/カ` と `ノ/の/之`。**引くときは揺れを作って複数回引き、
    照合するときは揺れをならしてから比べる**（`nameSearchVariants` / `normalizeName`）
  - ⚠ **引く回数に上限を置く**（3回）。1語で6通り投げると Nominatim の利用条件から外れる
  - ⚠ **並び順が「どれを選ぶか」を決めてしまう。** 同名の山は各地にあるので、
    検索結果は**いま見ている場所に近い順**に並べる。これが無いと、揺れを直しても
    利用者が遠い同名峰を選んでしまう
  - 検査は `tests/smoke_mapsearch.js`。⚠ **スタブは「表記が完全に一致したときだけ返す」
    ように書くこと。** 緩めると、揺れに対応していない実装でも素通りする
- **山名検索の情報源は OSM だけでは足りない。国土地理院を足す**（#13 から派生）。
  OSM(Nominatim)は日本の山名の網羅性が低い。「釈迦ヶ岳」で返るのは山梨の2件だけで、
  **矢板（高原山）の釈迦ヶ岳が出てこない**。⚠ **件数上限の問題ではない**——
  `limit=8` に対して2件しか返っていないので、上限を上げても増えない。
  その名前で索引に載っていないので、**情報源を足すしか手が無い**。
  笙ガ岳もOSM側では引けず、地理院側にはあった。
  - 地理院の地名検索 `msearch.gsi.go.jp/address-search/AddressSearch` を second source に
  - ⚠ **ブラウザから叩けるか（CORS）は未確認。** 落ちても黙って捨てる作りにしてあるので、
    読めなければOSMだけの結果に戻るだけで検索は壊れない。実機で確かめること
  - 情報源が違うと同じ峰でも座標が少し違う。**名前を正規化し約1km四方で畳む**
  - 検査は `tests/smoke_mapsearch.js`。⚠ 地理院を足さない版に戻すと3件落ちる
    （山梨の2件しか出ない＝報告どおりの症状）ことを確認済み
- **検索結果に標高を併記する。同名峰の選び分けは標高がいちばん確実**（#25 のマージ後、実機確認で判明）。
  「五竜岳」で地理院が同名を2つ返し、**名前と住所だけではどちらが北アルプスか分からなかった**
  （1,585m と 2,814m）。標高を出せば一目で選べる。
  - ⚠ **`fetchPointElevation` をそのまま使わないこと。** あれは `state.demElevation` を
    書き換えて表示ラベルまで更新するので、**選択中の地点の標高が候補の値で上書きされる**。
    副作用のない `readDemElevation` を切り出して使う
  - ⚠ **標高の読み取りを待って一覧を止めないこと。** 候補ぶんのタイルを引くので、
    先に一覧を出してから埋める。読めなければ空のままにする（一覧は出す）
  - ⚠ **`pickMapPoint` の第4引数（山頂標高）には渡さないこと。** ここで手に入るのは
    DEMの読みであって公称の山頂標高ではない。`state.summitElev` は areas.json の値の
    ための席で、DEMは `state.demElevation` に入って自動で使われる（`docs/spec/judge.md`）
  - ⚠ 地理院は **竜／龍のような漢字の揺れを自前で吸収**して返す（実機で確認）。
    こちら側で漢字の揺れを作る必要は無い。作るのは仮名の揺れだけでよい
- **検索結果の並べ替えは「近い順」を既定から動かさない**（#27 のあと）。標高順にしたいという
  要望に対して**トグル**で応えた。理由は2つ。
  ①**どちらが正しいかは検索ごとに違う。** 五竜岳は高い方が目当てだが、地元の低い「駒ヶ岳」を
  探しているときに木曽駒ヶ岳(2,956m)が先頭に来ては困る。
  ②**標高は後から非同期で埋まる。** 標高順を既定にすると一覧が出るまで待つことになり、
  「一覧を止めない」方針と衝突する。
  ⚠ **標高が埋まっても勝手に並べ替えないこと。** 押そうとした瞬間に行が入れ替わると
  押し間違える。並べ替えるのは利用者がトグルを押したときだけ。
  ⚠ 「高い順」で**まだ読めていない標高を 0 とみなさない**（逆順の先頭に来てしまう）。末尾へ。
- **百名山の印は「名前の一致」を必須にする。距離だけで判定しない**（#27 のあと）。
  百名山どうしは近接する（北岳と間ノ岳は約3km）ので、近いというだけでは隣の山の印を付ける。
  名前を正規化して一致を必須にし、距離（5km）は「同名の別の山を拾わない」ための保険に使う。
  ⚠ **色だけで区別しない。**「百名山」の文字も出す（色覚や画面の明るさで色は消える）。
- **漢字の揺れ（龍/竜・嶽/岳など）は照合と重複判定にだけ使う。検索語には足さない**（#27 のあと）。
  地理院は自前で吸収して返すので、検索語の揺れを作っても相手方を無駄に叩くだけ。
  畳めていないと**「五竜岳(OSM 2,811m)」と「五龍岳(地理院 2,811m)」が同じ山なのに2行出る**
  （実機で確認）。
- **公開URLの検証は「外部を叩かない検査」と「叩く検査」に分ける**（#32 / 2026-08-20）。
  ひとつのジョブで毎デプロイ走らせる案を、マージ直前にやめた。
  ①**sha256 の照合は外部を1つも叩かない**（GitHub Pages のみ）ので、赤の意味はひとつ
  ——「まだ配信されていない」。毎デプロイで自動に回してよい。
  ②**実ブラウザ＋Nominatim・地理院は手動のみ**。Nominatim の制限は**IP単位**で、
  Actions のランナーは他人のCIと共有の(Azure)IPを使うため、**こちらの行儀と無関係に
  絞られうる**。自動で回すと赤の意味が「壊した／相手が落ちた／IPが絞られた」に割れる。
  ⚠ **対応不要な失敗通知が定期的に届く検査は、そのうち誰も見なくなる。**
  このPRは「嘘の失敗を出さない」ために作ったのに、自動化そのものが嘘の赤の種になっていた。
  ⚠ 手で回しても「叩けるか」という情報は同じだけ得られる。**自動にして得るものより、
  偽の赤で失うものの方が大きい**と判断した。戻すのは `if:` 一行なので後戻りは安い。
  ⚠ 分けた代償は**回し忘れ**。`docs/workflow.md`「デプロイ後の確認」に
  「検索まわり・外部URLを触ったらマージ後に手で回す」と明記して塞いだ。
- **1回の検証で外に飛ぶ数は数えてから議論する**（#32）。「外部を叩く」と身構えたが、
  実測は Nominatim 1・地理院の地名検索 1・標高タイル最大8。
  ⚠ **地図タイルは0枚**（`verify-live.mjs` は `openMap()` を呼ばないので Leaflet が起動しない）。
  ⚠ `reverseGeocode` も0回（GPS成功時のみで、headless では位置情報が許可されない）。
  負荷そのものは論点ではなく、**論点は「赤の意味が割れること」**だった。
- **`--only` の綴り違いは既定に落とさず exit 2 で止める**（`verify-live.mjs`）。
  `--only=browsers` を黙って「両方」扱いにすると、CI側の意図と食い違ったまま緑になる。
  **検証していないものが検証済みに見える**のが、この手の道具のいちばん悪い嘘。
- **`playwright-core` は動的 import にする**（`verify-live.mjs`）。先頭で読むと、
  Chromium も `npm install` も要らないはずの `deploy` ジョブが**依存が無いというだけで落ちる**。
  遅延していることは、playwright-core が解決できない場所に置いて `--only=deploy` が
  通ることで確かめた（`--only=browser` は同じ場所で落ちる）。
- **タグは Actions から打つ。版数は「手で打った値」と「HTMLの表記」の二重入力で照合する**
  （2026-08-20）。**Claude のセッションからは `git push` でタグを送れない**（403）ため、
  タグ打ちだけが人の手に残り、実際 v4.77.0 から7つマージが進んでも止まったままになった。
  `.github/workflows/release.yml`（`workflow_dispatch`）で塞いだ。
  ⚠ **片方だけの検査では事故を止められない。**
  HTMLから読むだけ →「HTMLの上げ忘れ」をそのまま通す。
  入力を信じるだけ →「打ち間違い（v4.87.0 / v4.78.0）」をそのまま通す。
  だから両方を突き合わせる（`scripts/checkVersion.mjs`）。
  ⚠ **順番は「HTMLを上げてマージ → それからタグ」。** 逆にすると中身が古いまま
  新しいタグが付き、あとから見て食い違いに気づけない。
  ⚠ `docs/status.md` のズレは**警告どまりにして止めない**。1行目は自由文で版数以外の
  `v4.x.y` が混ざりうるため、落とすと**本質でない書式のズレでリリースが止まる**。
- **ワークフローの `run:` に `${{ inputs.* }}` を直に書かない**（`release.yml`）。
  式が**先に展開されてシェルのソースになる**ので、入力の書式検査をスクリプト側に
  置いても**呼ばれる前に負ける**。`env:` に受けて `"$VERSION"` と引用して使う。
  ⚠ `tests/smoke_version.mjs` で静的に見張っている（`env:` の代入行以外に
  `${{ inputs. }}` が出たら落ちる）。YAMLのコメントは展開されないので除外している。
  ⚠ 権限は `contents: write` 1つだけ。増やすとテストが落ちる。
- **意図的に壊して検査が捕まえることを確かめる**（今回も3通り）。
  ①入力を `run:` へ直に埋め込む ②権限を広げる ③`main` の確認を外す——
  いずれも `smoke_version` が落ちることを確認してから戻した。
  **書いただけの検査は、捕まえられるかどうか分からない。**
- **テストの走らせ方を書き写さない。`test.yml` を `workflow_call` で呼ぶ**（2026-08-20）。
  `release.yml` に `npm install` だけ書き写して **Chromium の用意
  （`playwright install` と `PW_CHROMIUM`）を落とし**、19件中14件が
  `executable doesn't exist at /opt/pw-browsers/chromium` で落ちた。
  ⚠ **同じ手順が2か所にあると、片方だけ直して食い違う。**
  `/opt/pw-browsers/chromium` は開発環境の同梱パスで、ランナーには無い。
  ⚠ `tests/smoke_version.mjs` が見張る（`release.yml` に `playwright` の語が出たら落ちる）。
  ⚠ **タグは打たれなかった。** 「テストを通っていない中身にタグを付けない」が
  設計どおり効いた。落ち方としては正しい。
- **`contents: write` はワークフロー全体でなくジョブに直付けする**（`release.yml`）。
  ⚠ **`contents: write` はタグだけでなくブランチにも書ける。** 門番やテストのジョブにまで
  配ると、必要のない手順が書き込み権限を持つ。既定は `read` にして、
  タグを打つジョブにだけ与える（`tests/smoke_version.mjs` が1か所であることを見張る）。
  ⚠ 書き込み権限を持つジョブは**版数を自分でもう一度確かめる**。門番が通っていても、
  権限を持つ手順の前提を他のジョブ任せにしない（1秒で済む）。
- **ワークフローを静的に検査するときは、先にコメント行を落とす**
  （`tests/smoke_version.mjs`）。YAMLのコメントは構文解析で捨てられるので効き目が無い。
  規則を説明した注記そのものを「違反」と読むと、
  **注意書きを厚くするほどテストが落ちる**という逆立ちが起きる（2回踏んだ）。
- **Node の版は `.nvmrc` 1か所に置く**（2026-08-20）。`node-version:` が6ワークフローに
  直書きされ、**`release.yml` だけ 22、他は 20 に割れていた**。
  ⚠ **手元は 22 なので、Node依存の不具合は「手元で通ってCIで落ちる」形になっていた。**
  `node-version-file: .nvmrc` に統一。⚠ `.nvmrc` は**手元の Node に合わせる**
  （予測が効くことが最優先。開発環境が上がったら一緒に上げる）。
- **Chromium の用意は共通アクション `.github/actions/chromium` に置く**（2026-08-20）。
  手順が3つ（`playwright install` / 実体を glob で探す / `PW_CHROMIUM` で渡す）あり、
  書き写すと必ずどれか落ちる。**`test.yml` と `verify-live.yml` に同じものが2つあった。**
  ⚠ `tests/smoke_workflows.mjs` が、ワークフロー側に `playwright` や `PW_CHROMIUM` の語が
  出たら落とす。**共通アクションを置いただけで誰も呼んでいない**状態も落とす。
- **`actions/checkout` と `setup-node` を v5 へ上げた**（Node 20 非推奨・2026-08-20）。
  ⚠ **警告が言う Node と、テストが走る Node は別物。** 警告はアクション自身の実行環境
  （`action.yml` の `runs.using: node20`）の話で、`node-version:` を変えても消えない。
  ⚠ **警告ゼロは目標にしない。** `actions/configure-pages@v5` は最新メジャーでも node20 で、
  上流が新しいメジャーを出すまでこちらでは消せない（`deploy-pages` / `upload-artifact` も同様）。
  ⚠ `ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION=true` は使わない。名前のとおり
  **安全でない古いランタイムに戻すだけ**で、警告だけ消えて状況は悪化する。
- **「同じことが2か所」は検査そのものにも当てはまる**（2026-08-20）。
  `smoke_version` にも Chromium の写し違い検査を書いていたが、`smoke_workflows` が
  全ワークフローで見るようになったので外した。**検査を写すのも重複に他ならない。**
- **気圧配置の H/L は「局所極値かつ起伏が一定以上」で拾う。縁は極値と呼ばない**（#26）。
  ⚠ **画面の端は「そこで見るのをやめた場所」**であって高気圧の中心ではない。
  通すと**地図を動かすたびに H が縁を滑る**という嘘の絵になる。
  #13 で「探索範囲の最高点」を山頂と取り違えかけたのとまったく同じ罠。
  ⚠ 縁の除外は**わざと二重**にしてある（ループ範囲／近傍8点の条件）。
  片方を消しても `smoke_pressure` は落ちないので、
  **「テストが通るから要らない」と判断しないこと**（検査は振る舞いを見ている）。
  ⚠ 起伏（`PRESS_PROMINENCE_HPA`=0.8）が無いと、モデルの数値の揺れを拾って
  H と L が画面中に乱立する。
- **等圧線の鞍部は4隅の平均で向きを決める**（#26）。marching squares のケース5/10は
  2通りに引けてしまう。決めずに固定すると**等圧線が×印に交差した図**になり、
  天気図として読めない。
- **気圧配置は風とズームの向きが逆**（#26）。風は拡大時のみ（`WIND_MIN_ZOOM`=7）だが、
  気圧は総観規模を見るものなので**上限**を設けた（`PRESS_MAX_ZOOM`=8）。
  ⚠ 拡大しすぎると画面内が一様になり線が1本も出ない。そのとき
  「線が出ない」ではなく**「広い範囲で見るものです」と理由を出す**。
- **数だけを見る検査は入れ替わりを見逃す**（`smoke_mapui` の気象レイヤー）。
  「5種」を「6種」に直すだけだと、1つ消して1つ足しても通ってしまう。**顔ぶれで見る。**
- **JSの静的検査も、先にコメントを落としてから見る**（`smoke_pressure`）。
  「`judgePoint` を参照しないこと」と書いた注意書きそのものを違反として拾った。
  ⚠ **注意書きを厚くするほどテストが落ちる**という逆立ちを 2026-08-20 に**3度**踏んだ
  （ワークフローのYAMLで2回、ここで1回）。**新しく静的検査を書くときは最初にこれを疑う。**
- **点で取る気象レイヤーの格子は「画面の四隅」でなく固定の升目に載せる**（#26 / 2026-08-20）。
  気圧配置を画面基準の格子で作ったところ、**少し動かすたびに点の緯度経度が変わって
  キャッシュが一度も効かず**、実機で `HTTP 429`（取りすぎ）を出した。
  `pressureBox()` が `PRESS_STEPS_DEG` から点数が上限に収まる刻みを選び、表示範囲を
  その倍数へ切り上げ／切り下げる。**地図タイルが固定の升目に切られているのと同じ考え方。**
  ⚠ **キャッシュの鍵に画面の座標を入れない**（升目と時刻だけで決める）。
  ⚠ **429 は取り直しても解けない。** 冷却期間（90秒）を置き、
  **あと何秒待つかを言葉で出す**（`HTTP 429` とだけ出さない）。
  ⚠ `windArrows` は同じ罠を抱えたまま（`WIND_GRID_N`=5 の25点で軽いので顕在化していない）。
  **触るときは一緒に固定升目へ寄せること。**
- **等圧線は高さごとに1レイヤーにまとめる**（#26）。1本の高さでも数十の線分に割れるので、
  線分ごとに `L.polyline` を作ると画面に数百のSVG要素が並び、地図を動かすたびに
  作り直して**目に見えて重くなる**（実機で「おもたい」と指摘）。
  Leaflet は線分の配列を1レイヤーとして描ける。**実測 108レイヤー → 9レイヤー。**
- **長方形の格子では行数と列数を分けて持つ**（#26）。片方だけで添字を計算すると
  **画面が横長のときだけ極値の位置が飛ぶ**という気づきにくい壊れ方をする。
  最初 `g.n` だけで書いていて、固定升目に変えて長方形になった時点で壊れた。
- **合成データの検査は「実装が実際に使う格子」に乗せる**（`smoke_pressure`）。
  傾き＋窪みの場を作ったが、升目の刻みが表示範囲で決まるため**格子点が窪みの底を外し**、
  極値が1つも出なかった。**距離だけの単調な場**にすれば、どの刻みでも
  「中心にいちばん近い格子点」が必ず唯一の極小になる。
- **気圧配置は画面の範囲でなく「総観規模の広さ」で計算する**（#26 / 2026-08-20）。
  画面の範囲だけで格子を作ると、**山を見る縮尺では等圧線が1本も画面に入らない**
  （実機で「大まかな気圧配置が把握しにくい」と指摘）。
  `PRESS_MIN_SPAN_DEG`(24度) まで中心を保って広げる。はみ出した線は地図が切る。
  ⚠ あわせて**ズームによる打ち切りをやめた**。「拡大したら消える」は使い勝手として逆。
  ⚠ **升目1つぶんの余白も足す。** 縁の点は極値と判定しないので、画面いっぱいに取ると
  **画面端の高気圧・低気圧が永久に出ない**。
- **等圧線の角張りは格子を細かくして直す。線を後から滑らかにしない**（#26）。
  実機で「輪郭がやたらと角張っている」と指摘。原因は格子の粗さ（広い範囲では3〜8度）。
  `refineGrid`（Catmull-Rom）で描画用に4倍細かくする。**通信は増えない。**
  ⚠ **線形補間では滑らかにならない。** marching squares 自体が辺の上で線形補間しているので、
  同じ形の折れ線が細かくなるだけ。3次で埋める必要がある（実際に線形へ落として確認した）。
  ⚠ 折れ線側を後から均す方法（Chaikin 等）は採らなかった。**隣り合う等圧線が
  触れたり交差したりしうる**ため。場を滑らかにすれば、線は原理的に交差しない。
  ⚠⚠ **補間は見た目を滑らかにするだけで精度は上がらない。** だから **H/L は元の格子から採る**。
  滑らかな線に釣られて「細かく分かっている」と思わせないための線引き。
- **「作った関数が正しいか」と「実際に使われているか」は別の検査**（#26）。
  補間の正しさは検査していたのに、`drawPressure` が補間した格子を使っているかは
  見ていなかった。**補間をやめて粗い格子で描くように壊してもテストが通った。**
  描いた結果（線分の数）から確かめるようにして塞いだ。
- **滑らかさは「頂点」ではなく「線分の中点」で測る**（`smoke_pressure`）。
  marching squares は辺の上で線形補間するので**頂点はほぼ理想の線の上に乗る**。
  角張って見えるのは頂点の間を直線で結んでいるからで、ズレは弦の中央に出る。
  最初は頂点で測ってしまい、粗い格子でもズレが0.067度しか出ずに検査にならなかった。
- **合成データの中心を格子点のちょうど中間に置かない**（`smoke_pressure`）。
  38/138 のような切りのよい値だと、刻みによっては4点が同着になり、
  どれも「近傍より真に低い」を満たさず**極値が1つも出なくなる**。半端な値にする。
- **計算した気圧配置レイヤーを外した**（#26 / 2026-08-20）→ `docs/adr/0010-pressure-layer-removed.md`。
  429・重さ・角張り・拡大で消える、を順に直しきったうえで、**それでも「実用に耐えない」**
  という評価だった。⚠ **個々の不具合ではなく形が噛み合っていない。**
  大まかな気圧配置には広い範囲が要る → 広い範囲は点数が増える → レートに当たる →
  点数を抑えると升目が300〜900kmになる、という三すくみ。
  ⚠ **補間で見た目は救えたが、情報量は粗い格子のまま**で評価は変わらなかった。
  ⚠ **パラメータの調整で越えられる壁ではない。** 点数の上限を上げる案は
  「利用者の回数を消費するので、こちらの都合で上げてよい数字ではない」として却下。
  次にやるなら①事前に計算して配る（`gen-outlook.mjs` と同じ形。CIで1日数回なら
  格子をずっと細かくできる）②気象庁の天気図を別パネルの1枚絵で出す（前線が読める）。
  ⚠ **地図に重ねるのは投影法が違うので不可**（H の位置が数百kmずれる）。
- **「直しきったが要件を満たさない」ときは、直した記録ごと残して外す**（#26）。
  3回の実機指摘をすべて直したうえでの撤退なので、**何を試して駄目だったか**が
  次の人にとっていちばん価値がある。コードは `6249445` に残っており、
  marching squares・Catmull-Rom 補間・局所極値の判定は**やり方を変えても使える部品**。
- **気象庁の天気図は「別パネルの1枚絵」で出す**（#26 / 2026-08-25）→ `docs/spec/ui.md`。
  ADR-0010 の代替案2を実装した。実測（`scripts/probeJma.mjs`）で分かったこと——
  `list.json` も画像も **`access-control-allow-origin: *`**、**24時間後・48時間後の
  予想天気図まで配信**、日本周辺(600x581)とアジア広域(600x512)の2種類。
  ⚠ **通信は一覧1回＋画像1枚**。格子点APIのような点数の問題が無く、三すくみが解けている。
  ⚠ **前線が描いてある**のが最大の利点。気圧の場だけからは原理的に出せない。
- **ファイル名の時刻はUTC。そのまま出すと9時間ずれる**（天気図）。
  さらに ⚠ **予想図の2つ目の時刻は基準時刻であって対象時刻ではない**（対象は基準＋24h/48h）。
  取り違えると「1日前の予想」と表示してしまい、**絵は正しいので気づけない**。
  `tests/smoke_wxmap.mjs` が両方を数字で固定している（8/25 12:00Z → 8/25 21:00 JST、
  基準 8/25 00:00Z の FSAS24 → 8/26 09:00 JST）。
- **一覧の並び順を仮定しない**（天気図）。昇順で返ってくるように見えても、
  名前から読んだ時刻で最新を選ぶ。順序が変われば**黙って古い絵を出す**ことになる。
- **`display` をインラインの空文字で戻さない**（天気図の画像）。
  CSS 側で `display: none` を指定していると、インラインを空にすると CSS が効いて
  消えたままになる。明示的に `block` を指定する（テストで踏んだ）。
- **調べるときは「中身の形」まで出す**（`scripts/probeJma.mjs`）。
  1回目は出力を先頭300文字で切っていたため、**予想天気図があることに気づけなかった**。
  取れた／取れないだけでなく、**何がどれだけあるか**まで見ないと画面を設計できない。
- **予報の確からしさは「目安」として出す。的中率の数字は作らない**（2026-08-25）
  → `docs/spec/ui.md`。「だいぶ先の予報は精度が怪しいと分かるようにしたい」という要望に、
  **リードタイム（何日先か）から引いた目安**で応えた。48時間未満は出さない／48〜120hは中／
  120h以上は低。
  ⚠⚠ **85%のような割合を根拠なく出さないこと。** 出さないより悪い——判断を誤らせる。
  `LEAD_TITLE` に「実測した的中率ではありません」と書き、`tests/smoke_lead.mjs` が
  **原文に `%` が出たら落とす**。
  ⚠ **近い予報には出さない。** 全部に出すと狼少年になり、本当に怪しいときの印が効かない。
  ⚠ **本物の信頼度は気象庁が週間予報に A/B/C で出している**（`bosai` 配下）。
  bosai は CORS が通ることを実測済み（#42）なので、**次はそれを引いて置き換えるのが筋**。
  そのときこの目安は消す（2つ並べると、どちらを信じるか分からなくなる）。
- **地図のピンはタップを一切受けない**（2026-08-31）→ `docs/spec/map.md`。
  Leaflet の既定のピンは `<img>` なので、**iOSでは長押しすると「画像として」
  共有／"写真"に保存のメニューが出る**（実機で指摘）。
  `interactive: false` ＋ `pointer-events: none` で**触ったら下の地図に抜ける**ようにし、
  `-webkit-touch-callout` / `-webkit-user-drag` / `user-select` も止めた。
  ⚠ **他のマーカーまで一括で止めないこと。** 百名山の△はタップで地点を選ぶので、
  `.leaflet-marker-icon` に一括指定すると**黙って死ぬ**（`smoke_pin` が見張る）。
  ⚠ `-webkit-touch-callout` は **Chromium に無い**ので `getComputedStyle` では確かめられない。
  テストは原文で見る。**実際に長押しメニューが出なくなるかは iOS 実機でしか分からない。**
- **気象庁の予報区（`office`）は山域でなく峰ごとに持つ**（2026-09-01）→ `docs/spec/judge.md`。
  信頼度は府県予報区単位でしか出ないので対応表が要る。
  ⚠ **山域に1つ持たせようとして13〜19件の「またがる」に悩んだが、それは持ち方の問題だった。**
  峰ごとに持てば判断はほぼ不要になる（稜線の東西で県が変わるのが実態）。
  ⚠ **ランキングは「その行が示している代表峰」（`bestPeakOf`）の office を使う。**
  最初「最高峰」という別の規則を作りかけたが、**ランキングは既に代表峰を持っていた**。
  規則を増やす前に、既にある概念を探すこと。
- **気象的にどちらもあり得るなら、利用者の認識と食い違わない方を採る**（2026-09-01）。
  平ヶ岳は分水嶺の上で、逆ジオコーダは群馬と答えた。気象的には群馬も正しいが、
  **「越後の山」という認識が強く、確認したときの齟齬が少ない**という理由で新潟にした。
  ⚠ 私は「両方出す」を推したが、**情報として正しくても現場で迷わせる**。
  正しさより、判断を助けるかどうかで選ぶ。
- **`areas.json` に列を足すときは、座標・標高・名前が変わっていないことを機械で確かめる**
  （2026-09-01）。110峰に `office` を足したとき、`lat`/`lon`/`elev`/`name`/`hyakumeizan`/
  `note`/`version` が1つも変わっていないことを差分ではなく**値の比較**で検算した。
  ⚠ 差分は末尾のカンマの付け外しで大きく見えるので、目視では確かめにくい。
- **気象庁の信頼度は「高/中/低」で出す。A/B/C の字を画面に出さない**（2026-09-02）。
  ⚠⚠ **アプリのA/B/Cは「登山適性」、気象庁のA/B/Cは「予報の信頼度」。**
  同じ字・同じ色で出すと**判定Cと読まれる**。元の記号は `title` に回す。
  ⚠⚠ **「降水確率と気温についての信頼度で、風の信頼度ではない」を必ず添える。**
  このアプリは風で判定するので、省くと**意味が反転して伝わる**。
  ⚠ `title` は素のテキスト。Markdown の `**` を書くとそのまま見える（一度書いてしまった）。
- **信頼度が空（1〜2日目）なら目安のままにする**（2026-09-02）。
  「無い」と「読めなかった」を混ぜない。空を空のまま出すと `気象庁の確度 ` という
  中身の無い表示になる（意図的に壊して確認した）。
- **テストの日付は「いま」から作る**（`smoke_lead`）。`forecastLead` は実際の現在時刻を
  基準にするので、固定の日付を書くと**過去になって打ち切られ、検査にならない**。
  実際に踏んで、fetch が1回も飛ばずに気づいた。
- **外部APIが返す数字は「意味」ごと変わる。値だけ見て分岐しない**（2026-09-07 / ADR-0011）。
  ADR-0006 の安全弁は Open-Meteo の応答の `elevation` を「風の場が立っている地面の高さ」
  として使っていた。2026-08 に皇海山で 1,405m を返した同じ地点が、2026-09 には 2,127m を
  返す。⚠ **標高の数字だけが山頂に寄り、風は地上10m風のまま。**
  結果、**110峰中109峰が黙って地上10m風へ落ちていた**。
  外部の数字で分岐するときは、**その数字が変わったら気づける検査**を一緒に置く。
- **「壊れた」ではなく「甘くなった」失敗は、利用者からしか報告が来ない**（2026-09-07）。
  至仏山の件はエラーも空白も出ていない。⚠ **判定Aが表示され、画面には
  `@地上10m（山頂2228m）` と正直に出ていた**。落ちたことを黙らない作りにしてあったのに、
  それだけでは気づけなかった。**落ちたことより「落ちた結果いくら見落としたか」**を
  出さないと、危険側のズレは伝わらない。
- **報告の空欄をまず埋める**（2026-09-07）。至仏山の報告で欠けていたのは
  「アプリが何 m/s と出していたか」だけだった。そこが空欄のままだと、
  解像度・地形加速・突風率と**原因の議論が全部あてずっぽうになる**。
  過ぎた日の予報値を引き直す道具（`scripts/probeWind.mjs`）を先に作った。
- **しきい値の調整で直そうとしない**（2026-09-07 / ADR-0011）。
  「100mが大きすぎた」なら50mにすればよいが、数えると50mで15峰・25mで71峰。
  ⚠ **どこに置いても外部の数字の意味に依存し続ける。**
  今回の失敗は「値が大きすぎた」ではなく「**その数字を見たこと**」だった。
  ⚠ ADR-0005 で踏んだ「入力が間違っているときに閾値をいじる」と同じ形。
- **死んだ引数を残さない**（2026-09-07）。`pickWindSource` は値を渡さないだけでも
  直るが、`modelM` の分岐が残ると**間違った前提がコードに書いてある**状態が続く。
  引数ごと消せば、渡そうとした時点で気づく。⚠ テストでも余計な引数を渡して
  **結果が変わらないこと**を見ている（黙って昔の挙動に戻らない）。
- **`kind` だけでなく `reason` まで見る**（2026-09-07 / `smoke_wind`）。
  「地上風に落ちる」検査を `kind === 'ground'` だけで書くと、**撤回したはずの
  安全弁で落ちていても通ってしまう**。実際、安全弁を消しても既存の検査は
  全部通った（別の理由で落ちていた）。⚠ **通り続けるテストは、直したことの証拠にならない。**
- **画面の幅は数えてから決める**（2026-09-09 / 凡例の値）。凡例に値を足す案を、
  「名前＋値でも入るだろう」と思って実装したら**実測で 515px 必要・366px しか無かった**
  （390px端末）。名前を落として色ドット＋値にして 345px に収めた。
  ⚠ 各系列の**名前と単位は既に軸が持っている**（気温°C・降水mm/h・風m/s・突風m/s）。
  凡例で二重に持つ必要が無かった、というのが本当のところ。
- **数字を並べるときは幅を固定する**（2026-09-09）。桁数が変わるたびに項目がずれると、
  なぞっている間じゅう凡例が横に踊る。⚠ **元の「窓が邪魔」より読みにくくなる**ので、
  値を出した意味が消える。`min-width` + `text-align: right` + `tabular-nums`。
  意図的に外して、実際に位置が動くことを確かめてある。
- **折り返しの検査を `offsetTop` の一致で書かない**（2026-09-09 / `smoke_legend`）。
  高さの違う項目が中央揃えされると**1pxずれて誤検知**する（実際に踏んだ）。
  「全項目の縦の範囲が重なっているか」で見る。折り返せば2行目は1行目の下端より下に来る。
- ⚠⚠ **未コミットの変更があるうちに `git checkout -- <file>` を使わない**（2026-09-09）。
  意図的に壊して検査が落ちることを確かめる手順で、戻すつもりの `git checkout` が
  **自分の実装ごと消した**。壊す前に必ずコミットする（wipで構わない）。
- **窓を小さくするときは「面積」で比べる**（2026-09-09 / ポップアップ）。
  時刻と「天気＋判定」を1行にまとめたら高さは31px縮んだが、⚠ **幅が 133→189px に太った**
  （時刻＋絵文字＋判定の札で1行が長くなる）。窓が覆うグラフの量で見ると負けるので戻した。
  行数だけ数えて「削れた」と思わないこと。
- **見出しを絵文字にしない**（2026-09-09 / ポップアップ）。利用者の案は絵文字だったが、
  ⚠ **上の行に天気の絵文字が出ている**ので、見出しまで絵文字にすると
  「どちらが天気か」が一瞬わからなくなる。単漢字（温・雨・風・圧）にした。
  幅も1文字ぶんで足りる。
- **`user-select: none` を `body` へ一括指定しない**（2026-09-09）。iOSの虫眼鏡を止めるとき、
  ⚠ **地図の緯度経度（#13でコピーさせるために入れた `user-select: all`）を巻き添えにする。**
  指を置いてなぞる場所（`#charts-outer` / `#scrubber`）だけに絞る。
  ピンのときの「一括指定すると百名山の△が黙って死ぬ」と同じ形（PR #52）。
- **窓の大きさの検査は px ではなく割合で書く**（2026-09-09 / `smoke_popup`）。
  端末と字体で変わる。チャート領域に対する割合にして上限を置く。
  ⚠ 上限を 40% にしたら余裕が2%しか無かった。**字体が変わるだけで落ちるのは検査ではなく事故**
  なので 45% にした（昔の姿は61%なので確実に落ちる）。
- **「描くコードがある」と「画面に出ている」は別**（2026-09-09 / 標高の札）。
  標高の札は前から描いてあったが、⚠ **チャート本体の左端**——全幅3301pxの先頭——に
  描いていた。通常のスクロール量は800px超なので**事実上ずっと画面の外**だった。
  利用者に「表示して欲しい」と言われるまで気づけなかった。
  スクロールする面に置いた目印は、**なぞった先でも見えるか**を必ず確かめる。
- **切れる場所での「はみ出し」は幅では測れない**（2026-09-09 / `smoke_elev`）。
  ガター（36px）からはみ出した札も、キャンバスに切られて「幅36px」としか測れない。
  ⚠ 実際にこれで検査が空振りし、**わざと溢れさせても通ってしまった**。
  「左端に貼り付いていたら切れている」で見る。収まっていれば必ず余白ができる。
- ⚠⚠ **死んだ関数は、次に来た人が必ず踏む**（2026-09-09）。降水バーの縁取りを
  `drawPrecipBars()` に書いたが、**この関数はどこからも呼ばれていなかった**。
  実際に描いているのは `drawCloudPrecip()` の中。名前は素直で中身もそれらしいので、
  **私自身が探して見つけて信じ込んだ**。画素で見るテストが拾ってくれた。
  ⚠ **「それらしいコードを見つけた」で終わらせず、呼ばれているかを確かめる。**
  踏んだので、その関数は消した（45行）。
- **印は「付かないこと」を検査する**（2026-09-09 / `smoke_mark`）。
  判定の色が付くことだけ見ていると、⚠ **全部に付いていても通ってしまう**。
  Aしかない日を用意して**1画素も付かない**ことを見る。
  どの時間にも赤が出れば狼少年になり、印そのものが効かなくなる。
- **見た目の閾値と判定の閾値がずれていることがある**（2026-09-09）。気圧パネルは
  |ΔP6h|≧3 から警告色が付くが、⚠ **ABC判定のBは6・Cは10**。
  「画面は警告なのに判定はA」という食い違いが前からある。**未整理**。
- **印は「地面に見えないか」まで見る**（2026-09-09 / 降水の縁取り）。
  バーを四角で囲ったら、⚠ **下の辺が地表帯と重なって「地面の線」に見えた**（実機で指摘）。
  左・上・右の3辺だけの**コの字**にした。囲むこと自体が目的ではない。
- **行どうしを比べる検査は「同じものを横切る行」で比べる**（2026-09-09 / `smoke_mark`）。
  「最下行 vs 真ん中の行」で比べたが、⚠ **真ん中の行は背の高いバーしか横切らない**ので
  数が減るのは当たり前で、検査になっていなかった。最下行と**その8px上**で比べる。
  どちらもすべてのバーを横切るので、差が出れば下の辺があるということ。
- **気圧の色と判定の閾値の食い違いは、いまは直さないと決めた**（2026-09-09 / 利用者の判断）。
  気圧パネルは |ΔP6h|≧3 から警告色が付くが、⚠ **判定のBは6・Cは10**。
  「画面は警告色なのに判定はA」という状態が残る。**体感温度を描くほうが先**という判断。
  ⚠ 直すときも動かすのは**見た目だけ**。判定の閾値は動かさない（ADR-0005の罠）。
- **重なる印は「危ない方を後に描く」**（2026-09-09 / 降水の縁取り）。
  隣り合う棒の縁は重なる。1周で描くと⚠ **時刻が後ろの棒が勝つ**ので、
  Cの隣がBだと**赤の上に橙が乗った**（実機で指摘）。B→Cの2周に分けた。
  ⚠ 検査は画素を狙い撃ちせず、**赤の総量が減らないこと**で見る
  （隣がA の場合と隣がB の場合を比べる）。順序を戻すと赤が3割食われる。
- **色の目盛りは固定にする**（2026-09-10 / 体感温度の帯）。
  その日の最低・最高で伸縮させると**穏やかな日の帯まで真っ赤・真っ青**になり、
  危なさを誤って伝える。⚠ 検査でも実害が出た——伸縮させると
  **Aしかない日の塗りが `GRADE_COL.C` と同じ赤**になり、枠と見分けが付かなくなる。
  ⚠ 「どの日に開いても同じ色＝同じ寒さ」を、日ごとの見やすさより優先する。
- **重なる印を作るときは内寄せしない**（2026-09-10 / 体感温度の帯の枠）。
  隣り合う区間の縦線を左右 1px ずつ内側へ寄せていたため**そもそも重ならず**、
  「赤が橙に勝つ」の検査が空振りしていた。⚠ 順序の決まりを守らせたいなら、
  まず**重なる形にしておく**こと。内寄せを外したら、順序を入れ替える破壊で落ちるようになった。
- **`FEEL_BAND_H` は3か所（定義＋uPlotのpadding＋ガターのskyBottom）で対**（2026-09-10）。
  片方だけ直すと気温の線が帯に潜る。⚠ 検査は**原文に両方の式があること**と、
  **`skyChart.bbox` の下端が帯の上端以下であること**の両方で見る（片方だけだと通る）。
- **色の濃さは「判定がある側」に配る**（2026-09-10 / 体感温度の帯）。
  体感温度の判定は**寒い側だけ**（`abcScoreInv`）。なのに 10℃ を中立にして上下へ均等に
  濃くしたら、⚠ **体感26〜31℃のふつうの夏日で帯が終日べったり橙**になり、判定はAのまま——
  `GRADE_COL.B` と紛らわしく「Aなのに警告色」に見えた（実機の画で確認）。
  8〜33℃ を淡い無彩色にし、濃さの大半を寒い側へ移した。
  ⚠ **判定に閾値が無い側へ濃い色を出さない**、が一般則。
- **色の変化点を判定の閾値と同じ数値にする**（2026-09-10 / 体感温度の帯）。
  寒い側は -3（判定B）/ -15（判定C）。⚠ 検査で `THRESH` と `FEEL_STOPS` を突き合わせ、
  **片方だけ変えたら落ちる**ようにした。気圧（|ΔP|≧3 で色・判定のBは6）と同じ轍を踏まない。
- **熱中症の基準は WBGT（暑さ指数）で、気温ではない**（2026-09-10 / 調べもの）。
  熱中症警戒アラート = WBGT 33、特別警戒 = 35（環境省）。日常生活の指針は
  危険31 / 厳重警戒28 / 警戒25 / 注意21。湿度60%で 気温31℃ ≒ WBGT 28、気温35℃ ≒ WBGT 32。
  ⚠ **Open-Meteo の `apparent_temperature` は WBGT ではない**（日射込みで気温よりかなり高く出る。
  実機で気温25℃→体感31℃）。公式の換算が無いので、暑い側の色の数値は**暫定**のまま。
- **`margin-left:auto` は「flex の子」に付ける**（2026-09-10 / 版数を about.html のリンクにしたとき）。
  版数の span を `<a>` で包んだら、⚠ **flex の子がリンクに移り**、
  版数が行の右端に寄らなくなった（`smoke_favpicker` の「行の右端に寄っている」が拾った）。
  要素を包むときは、**寄せ・伸縮の指定が誰に付いているか**を必ず見る。
- **書き写した数字は「行ごと」に突き合わせる**（2026-09-10 / about.html の閾値）。
  最初は本文全体への `includes` で見ていたが、単位を項目名側へ出して値が短くなった結果、
  ⚠ **風速の「5 未満」が降雪の「0.5 未満」に含まれて**、風速の行が間違っていても通った。
  表を行に分け、その行のセルが期待どおりかを見る形に直した。
- ⚠⚠ **また `git checkout -- <file>` で未コミットの実装を消した**（2026-09-10・3回目）。
  意図的に壊す手順の直後。about.html の CSS 修正と表の組み直しが両方消えた。
  **壊す前に必ずコミットする**（wip で可）。3回とも同じ形で踏んでいる。
- **説明ページは「配るもの」ではなく「自分が説明するための資料」**（2026-09-10）。
  最初は友人に渡す体裁で作ったが、利用者の意図は
  「尋ねられたときに、ここからこう引いてこう解釈している、と説明するため」だった。
  ⚠ 向きが変わると**必要な中身も変わる**——免責より**妥協点**が主役になり、
  GitHub への案内は不要になった（ADR-0009 の記述に辿れるので、外したほうが良い）。
- **フッターの6つ目には文字を入れない**（2026-09-10）。
  「説明」と入れると **360px 幅で全ボタンが2行になり、フッターが 47px → 61px に伸びる**。
  アイコンだけ＋`flex: 0 0 auto` なら 360px でも崩れない。
  ⚠ **原文を見ても分からない**ので、描いて高さを測る検査にした。
- **PRECACHE に足すだけなら `CACHE_VERSION` は上げない**（2026-09-11 / about.html）。
  sw.js の中身が変われば install が走り、**同じキャッシュに追加される**。
  ⚠ 上げると既存のアプリシェルを丸ごと捨てて取り直しになる（圏外で開けない時間が生まれる）。
  上げるのは「本体を大きく変えたとき」だけ。
- **PRECACHE のファイルが実在するかを検査する**（2026-09-11）。打ち間違いを拾うため。
  ⚠ ただし `data/spots.json` は**まだ生成していない**ので除外してある（Issue #8）。
  `cache.add` は握りつぶすので実害は無い。生成したら除外を消すこと。
- **回転した地図の中では「自分の transform」＝「画面上の向き」ではない**（2026-09-11 / ヘディングアップ）。
  ⚠ 矢尻は `mapRotationDeg` を**足していて二重**、風向の矢印は逆回転する箱の中で
  **地図の回転を失っていた**。実機で「逆に進んでいるみたい」と指摘されて判明。
  ⚠⚠ **既存の検査2つがバグの側を正解として書かれていた**——どちらも要素自身の
  `transform` しか見ておらず、`rotate(0deg)` を「上を向いている」と解釈していた。
  祖先ぶんを掛け合わせた**画面上の向き**で見ること。
- **地図の回転に追従させる角度は CSS 変数で持つ**（2026-09-11 / 風向の矢印）。
  ⚠ JS で `rotate(Xdeg)` を焼き込むと、**地図が回っても矢印は焼いた角度のまま**。
  `rotate(calc(var(--wind-deg) - var(--map-rot)))` にすれば、方位が変わるたびに追従する。

## 2026-09-13 タグを自動で打つ（版数の二重入力をやめ、検査に置き換えた）

- タグが v4.85.0 で止まり配信物は v4.90.0。**利用者が版数を「4.98.0」と読み違える**
  ところまで来た。タグ打ちだけが人の手に残っていたのが原因
- `release.yml` に `workflow_run`（`Pages へデプロイ` の completed）を足した。
  ⚠ **成功したデプロイのときだけ**（`conclusion == 'success'`）。workflow_run は成否によらず届く
- ⚠ **見る中身は `workflow_run.head_sha` に固定。** 既定ブランチの先端を見ると、
  デプロイ後に進んだ別のコミットへタグが付きうる
- ⚠ **main 判定は `workflow_run.head_branch`。** workflow_run のときの `GITHUB_REF_NAME` は
  起動元の枝ではなく**既定ブランチ**になるので、それで判定すると素通りする
- ⚠ **自動経路では「既にあるタグ」で落とさない。** 毎デプロイ走るので、
  版数を上げていないマージのたびに赤が付き、本物の失敗が埋もれる。
  `should_tag` に落として先のジョブを走らせない。手動経路は逆に落とす
- ⚠ **二重入力をやめたぶんの穴。** 手で版数を打たせていたのは「打ち間違い」と
  「HTMLの上げ忘れ」の2つを止めるため。人が打たなくなれば前者は消えるが**後者は残る**。
  `scripts/checkVersionBump.mjs` が `test.yml` の `bump` ジョブ（PR のときだけ）で見る
- ⚠ **版数は数として比べる。** 辞書順だと `'v4.9.0' < 'v4.10.0'` が偽になり、
  **10番台に入った瞬間に上げ忘れを見逃す**
- ⚠ 比較先が取れないときは**通す**。ここは門番ではないので、
  浅いクローンなどで読めないときにリリースを止めない
- 読み取りは `scripts/lib/appVersion.mjs` に一本化。ワークフローからは
  `checkVersion.mjs --print`。⚠ YAML に `grep` を書くと同じ式が2か所になる
- ⚠ `release.yml` と `pages.yml` は**名前の文字列で繋がっている**。`name:` を変えても
  GitHub は何も言わず黙って切れるだけなので、`smoke_workflows` で突き合わせるようにした
  （`verify-live.yml` も同じ名前を待っていたことがこの検査で分かった）
- 7つ意図的に壊して、検査が落ちることを確かめた

## 2026-09-13 圏外でも直近の予報を出す（アプリ側で控える）

「オフラインでも10分に1回くらい自分の位置の天気をチェックして、電波が無くても
直近の電波が繋がった場所での情報を確認できないか」という相談から。

- **「アプリを閉じた状態での定期取得」は作れない。** iOS は Background Sync /
  Periodic Background Sync / Background Fetch のいずれも未対応で、バックグラウンドに
  回すとページと Service Worker の JS ごと止まる。Android の Periodic Background Sync も
  **最短12時間**で、10分間隔は元から無い。バックグラウンドでの位置取得も Web に無い
- ⚠ **そもそも10分ごとである必要が薄かった。** 1回の取得で `past_days=3` /
  `forecast_days=9` ぶん手元に来る。足りないのは「新しい予報」ではなく
  「**取ったものを保持していないこと**」だった。埋めたのはそこだけ
- **`sw.js` ではなくアプリ側に置いた。** SW で気象APIをキャッシュすると
  「古い数字が黙って出る」状態になり、ADR-0011 と同じ形の事故になる。
  古さと取得地点を画面に出せる場所が要る
- 保存は IndexedDB `sotoki-wx`。**控えるのはAPI応答そのもの**（整形後ではない）。
  復元も通信時と同じ `processData` を通す（`applyWeatherJson` に一本化）
- 期限は **24時間**（利用者判断）。超えたら数字を出さず「古すぎる」とだけ言う
- 完全一致が無ければ **20km 以内**の最寄りを流用する。⚠ **0にはできない**——
  GPSは毎回わずかに違う座標を返すので現在地は完全一致しない。
  そのかわり**離れている距離を必ず帯に出す**
- ⚠ **IndexedDBのトランザクションは `await` で閉じる。** 本体の保存と補助データの
  後付けが同時に走ると、補助のほうが保存前の古い `at` を書き戻し、直後の整理で
  期限切れとして消えた。読みと書きを1つのトランザクションに収めた（`wxUpdate`）。
  `smoke_offline` を書いていて見つけた
- ⚠ 帯（`#offline-note`）は**描画の予約より先に出す**。あとから出すと
  そのぶんの高さがチャートに入らず下端が隠れる
- ⚠ **`code_map.md` の「約N行」は以前からずれている**（THRESH が「約1930」で実際は
  約2290 など、360行ほど）。今回足した節だけ実測値を入れた。
  索引ごと直すのは独立した作業として残す

## 2026-09-19 圏外の控えの地点名が別の山になる（v4.91.0 の実機で発覚）

機内モードで吾妻あたりを選んだら、帯が「女峰山（19.2km先）」と出た。
女峰山は日光で約127km離れており、20km以内しか流用しない作りと矛盾する。
→ **座標は正しく、名前だけが別地点のものだった。**

- 原因は `saveWxCache` が地点名だけ **`state.locationName` を保存時に読み直していた**こと。
  緯度経度は `fetchWeather` の引数で固定されるが、名前はグローバル。
  通信とIndexedDBの往復のあいだに別の地点を選ぶと、**後から選んだ名前が
  先に取った地点の控えに焼き付く**
- 直しは `fetchWeather` の冒頭（awaitを挟む前）で `reqName` に写し取り、引数で渡すだけ
- ⚠ **引数と、あとから読むグローバルを混ぜない。** 片方だけ時点が違うと、
  今回のように「座標は正しいのに名前が別」という**気づきにくい形**で壊れる
- `smoke_offline` に回帰検査を足した。わざと戻して落ちることも確認済み
  （取得中に `state.locationName` を書き換え、控えと帯の両方を見る）
- ⚠ **20km の流用そのものは別の論点として残る。** 山では19.2km先は別の天気になりうる。
  距離は帯に出ているが、閾値を詰めるかは実機の使用感を見てから

## 2026-09-19 地図に縮尺のメジャーを足した（v4.93.0）

「ヘディングアップで自機を追跡しながら拡大縮小したとき、いま画面のこの長さが
何kmなのか知りたい」という要望から。

- ⚠⚠ **Leaflet の `L.control.scale` は使えない。** ヘディングアップは `#map` 要素ごと
  CSS の `rotate()` で実現しているので、地図の中に置くと**目盛りごと傾いて読めない**。
  `#map-bottom`（`#map` の外）に自前で置いた
- 置き場所は**右下・出典の真上**（左下は雨の予告・地点名・出典で混んでいる）。利用者判断
- **1km未満は m、1km以上は km**。利用者判断（「0.2 km」より「200 m」が読みやすい）
- 数字は `1 / 2 / 5 × 10ⁿ` に丸める。337m のような中途半端な数字は読み取れない
- ⚠ **1pxあたりの距離は緯度で変わる**（メルカトル）。倍率だけの固定式にせず、
  画面上の2点を緯度経度へ戻して実測する。北海道と八重山で1割以上ちがう
- ⚠⚠ **棒に `transition` を付けない。** 最初は「ぬるっと伸び縮み」させたが、
  拡大縮小の途中で**棒の長さと数字が食い違う**ことを検査が捕まえた。
  メジャーは読み取る道具なので、一瞬でも嘘の長さを出してはいけない
- `smoke_mapscale` で見ているのは「出るか」ではなく**「棒の長さと数字が合っているか」**。
  棒のピクセル幅を緯度経度へ戻して実測し、ラベルと突き合わせる。
  固定式にする／丸めをやめる の2通りに壊して、落ちることを確認済み

## 2026-09-19 ヘディングアップ追跡中に画面を消さない（v4.94.0）

「ヘディングアップで自機追跡している際に自動でスリープが効いてうざい。
充電している最中はスリープ機能を切れるか」という要望から。

- **スリープを止めるのはできる**（Screen Wake Lock API・Safari iOS 16.4+）
- ⚠⚠ **「充電中だけ」は作れない。** Battery Status API を WebKit が実装していない
  （指紋採取のリスクとして見送られ、一度も出荷されていない）。iOS はサードパーティの
  ブラウザも WebKit 強制なので、Chrome を入れても取れない。
  → 条件は「ヘディングアップ＋追跡のあいだは常に」にした（利用者判断）
- ⚠⚠ **「あと◯分でスリープに移行します」も出せない。** OS の自動ロック設定は
  Web から読めない。**分からない数字を書くと嘘になる**ので、時間は出さず充電をすすめるに留めた
- ⚠⚠ **ホーム画面のPWAでは iOS 18.4 未満で効かない**（Apple が 18.4 で直したバグ）。
  効かないのに「消しません」と出すと、**消えないつもりで山で画面が落ちる**。
  握れなかったら帯を警告色にして「この端末では画面が消えます」と正直に出す
- ⚠ 画面を隠すと勝手に解放される。`visibilitychange` で取り直し、`release` で表示も落とす
- ⚠ モードを抜けたら「やめる」の選択も戻す。持ち越すと次に山で使うとき黙って画面が落ちる
- `smoke_awake` の本丸は「消さないか」ではなく**「嘘をつかないか」**。
  取れないのに「消しません」と言う／「やめる」を持ち越す の2通りに壊して落ちることを確認済み

## 2026-09-19 タイルの取得失敗を地図の上に出す（v4.95.0）

ヘディングアップ・自車追跡中に、地図へ細い筋が2本出るという報告。
再現できなかったので、まず**原因を切り分けられる状態**を作ることにした（利用者判断）。

- ⚠⚠ **下地（ベースマップ）に `watchTileStatus` が付いていなかった。**
  気象レイヤーとオーバーレイだけ見張っていて、**地図そのものが取れていなくても
  誰も気づかない**状態だった。`applyBaseLayer` で付けた
- ⚠⚠ **失敗の知らせがレイヤーパネルの中だけだった。** 走行中に開く人はいない。
  `#map-trouble` の帯で地図の上にも出す（押すとパネルが開いて内訳が見える）
- ⚠⚠ **「この範囲に表示なし」を警告に混ぜない。** 雷は元からタイルが無いことがある。
  失敗と同じ顔で出すと狼少年になり、本物の失敗を見なくなる。
  `setLayerStatus` に第3引数 `isError` を足して区別した
- ⚠ 筋の原因はまだ**分かっていない**。候補は（A）タイルの継ぎ目（B）1行ぶんの
  取得失敗。この変更は（B）なら原因を名指しできるようにするもので、（A）は直らない。
  再現の試み（回転9通り・単層／合成オーバーレイ・細い筋の検出）はすべて空振り
- `smoke_tiletrouble` は、下地を見張らない／「表示なし」を失敗扱いする の2通りに
  壊して落ちることを確認済み

## 2026-09-19 縮尺のメジャーを左上へ移し、大きくした（v4.96.0）

「右下だと読みにくい。左上に移して、表示サイズももっと大きく」という要望。

- 置き場所: 右下・出典の真上 → **左上・検索行の下**
- 大きさ: 棒の上限 110 → **150px**、太さ 6 → **10px**、文字 10 → **15px**
- ⚠ **`#map-topleft` という縦積みの器を作り、`#map-when` も一緒に入れた。**
  同じ左上に2つ出るので、別々に絶対配置すると重なる
- ⚠⚠ **器は `#map-results` より下に重ねる**（z-index 1090 < 1100）。
  検索結果と同じ場所に出るため、上に重ねると**結果が読めなくなる**。
  `#map-when` はこれまで結果より上（1100）だったので、ここだけ挙動が変わっている
- ⚠ **検査で棒の長さの下限を固定値にしてはいけない。** 1/2/5 に丸めるので、
  棒の長さは上限の40〜100%のあいだで毎回変わる。最初 100px 以上で書いて落ちた
  （実際の長さは82px）。**上限の定数**を見るように直した
- 左上・上寄せ／文字と上限の大きさ／結果より下に重ねる の3通りに壊して、
  落ちることを確認済み

## 2026-09-19 レイヤーの状態は「旗」ではなく「関数」で分ける（v4.96.1）

v4.95.0 で `setLayerStatus(id, text, isError)` の第3引数を足し、既定を
「文言があれば失敗」にした。タイルの経路には旗を付けたが、**点で描くレイヤー
（山域・アメダス・風の矢印）の「この範囲に山域がありません」「もう少し拡大すると
表示されます」「取得中…」には付け忘れた**。結果、実機で地図の上に

    ⚠ 地図が取れていません　山域・百名山：この範囲に山域がありません

が出た。狼少年をやめるために作った仕掛けが、そのまま狼少年になった。
付け忘れた箇所は8つ（うち2つは「取得中…」）で、**平地で地図を開けばほぼ常時**
赤帯が出る状態だった。

→ 旗を廃止し、`setLayerError` / `setLayerNote` / `clearLayerStatus` に分けた。
  **どれを呼ぶか選ばないと書けない**ので、付け忘れようがない。

⚠ **検査の書き方も同じ穴に落ちていた。** v4.95.0 の `smoke_tiletrouble` は
`setLayerStatus('thunder', '…', false)` と**旗を自分で渡して**いた。仕掛けだけを
見て呼び出し側を見ていないので、呼び出し側の付け忘れは永久に捕まらない。
→ 山域レイヤーを点けて山域の無い所へ動かす、という**アプリと同じ道**で見るように
  直した。加えて、旗つきの `setLayerStatus(` が本体に残っていないことも見る。

## 2026-09-19 積算値は「先の1時間」に揃える（v4.97.0）

実機の報告：20:34 に見たら 20:00 が「雨 3.7mm」なのに降っていない。

Open-Meteo の `precipitation` / `snowfall` は**直前1時間の合計**、
`wind_gusts_10m` は**直前1時間の最大**。いっぽう気温・気圧・風・雲量は
**その時刻の瞬間値**。`processData` は両方をそのまま `time[i]` の行に詰めていたので、
**1つの行に2つの意味が混ざっていた**。`indexOfNow` は「now 以下で最大の時刻」を
返すため、20:34 にはカーソルが 20:00 に乗り、**19〜20時に降った量**を
"いまの雨"として出していた。

→ `aheadHour(arr, i)` で `arr[i+1]` を取り、行の意味を
  **「その時刻から1時間のあいだに降る／吹く」**に揃えた。

⚠ **判定の入力そのものが動く。** `judgePoint` / `abcScore` / `THRESH` は一切
触っていない（規約どおり移植もしていない）。変わるのは**どの時間の雨がどの行に
入るか**だけ。そのぶん v4.96.1 以前に見た判定とは1時間ぶん食い違う。
利用者に選んでもらったうえで実施（選択肢は「先の1時間に揃える／表示だけ直す」）。

⚠ **`processData` は本体のグラフと山域ランキングの共通の入口。** だからここ1か所で
両方そろう。逆に言えば、ここを触ると**ランキングのABCも同時に動く**。

⚠ **補助リクエストの突風も同じ分だけずらすこと。** `applySupplemental` は時刻文字列で
突き合わせるので、素の `d.time` で引くと**補助が通った端末だけ**後から1時間ずれた値で
上書きされる（JMAは突風を返さないので、実機では必ずこちらが効く）。

⚠ **先が無い最後の行は `null`。** 0 を置くと「分からない」を「0mm」と断言することに
なる。判定側は `d.precip > 0` と `|| 0` で受けるので null のまま通る。

### ずらしていないもの

- **瞬間値**（気温・体感・気圧・風・風向・雲量）… 元からその時刻の値
- **`snowRanking.js`**（新雪ランキング）… 自前で `hourly` を取り、**窓ごとの合計**を
  出す別の仕組み。合計なので1時間ずれても窓が1つ動くだけで、しかも補正係数 `k` は
  同じ窓の比なのでズレが打ち消し合う。`docs/snow_ranking.md` に独立と明記されており、
  期待値つきの検査もあるため、**報告された症状と無関係な所まで広げない**判断。
  直すなら別途、専用の検査とセットで。

## 2026-09-19 レーダー実況とモデル予報を突き合わせる（v4.98.0）

グラフに出ているのは JMA MSM の**予報**。1時間のズレ（v4.97.0）を直しても、
「降っていないのに雨」はモデルが外れれば起きる。**直せないなら隠さない。**

気象庁の高解像度降水ナウキャストは**レーダーの実況**で、アプリは既に地点の1画素を
読んでいた（雨の予告）。それをグラフにも使う。

### 実装で踏んだもの（全部、既存の検査が捕まえた）

1. **`buildCharts()` を `render()` の外で呼んではいけない。**
   読み込み直後は `applyWeatherJson` の `render()` が rAF で**あとから**走るので、
   uPlot を二重に組み立ててチャートが壊れた（雲パネルの標高の破線が消え、
   `smoke_elev` が落ちた）。描き直しは `applySupplemental` と同じ三点セットに揃え、
   読み込み直後は**描き直さない**（あとから来る `render()` に任せる）。

2. **帯を出すとチャートが縮む。** `#offline-note` と同じ flex 列に入るので、
   帯を1本増やすとそのぶん描画領域が減る。**通信で取れなかっただけで帯を出すと、
   圏外では帯が2本**になって無駄に縮む。
   → 失敗を `kind` で分け、`'device'`（画素を読めない＝待っても直らない）だけ言う。
   `'network'` は黙る（圏外の帯が既に出ている）。

3. **読み手が2人になったら、読みを使い回す。** 「雨の予告」と「突き合わせ」が
   別々に読んで**タイルを13枚×2＝26枚**取りに行った（`smoke_rain` が捕まえた）。
   60秒のキャッシュで1回ぶんにした。

4. **突き合わせるのは「いまこの瞬間」。行の時刻ではない。**
   行の時刻はその1時間の**始まり**なので、23:47 に見ると行は 23:00。ナウキャストは
   「いま＋60分」なので、行の時刻で引くと**最大59分ぶん過去に外れて** null になり、
   食い違っていても黙ってしまった。モデルは「いまの1時間」、レーダーは
   「いまこの瞬間」で比べる。

### 検査を直したところ（意味が変わったもの）

- `smoke_rain`「地図を閉じている間はナウキャストを取りに行かない（0枚）」
  → **0枚には戻せない。** グラフが実況を要るようになったため。元の狙い
  「同じ並びを何度も取りに行かない」は残し、**1回ぶん（13枚以下）**を見る形にした。
  圏外では0枚、という検査を足してある。
- `smoke_snowui` の通信回数カウンタが `jma.go.jp` を**全部**数えていた。
  無関係な通信が1本増えただけで落ちるので、**用途（アメダス）で数える**ように絞った。

### 分からないことを断言しない

`radarWetAt` は範囲外を **null** で返す。グラフの実況ストリップも範囲外は**描かない**。
描いてある区間＝見に行けた区間、という読み方ができる。

## 2026-09-20 雲の着色（`SAT_TINTS`）を撤去した（v4.99.0）

白黒の赤外バンドをピンク／シアンで塗る機能。v4.75.0 で入れ、v4.75.1 で
「フィルタを要素ごと作り直す」修正も入れたが、**実機（iOS）では最後まで
白のままだった**（利用者の報告・issue #2）。利用者から
「赤外／雲頂だけにした方がシンプルかも」と提案があり、そのとおりにした。

### なぜ直さずに消したか

- ⚠ **Chromium では正しく色が付く。** 画素で測って ピンク(255,46,184) /
  シアン(51,242,255) が出ることまで確認した。フィルタも押すたびに
  `satAlpha1→2→3` と作り直されている。**ロジックは正しい。**
  つまり**ヘッドレスでは永久に再現しない**不具合で、検査を足しても捕まえられない。
- ⚠ **直す道も塞がっている。** アルファに色を流し込む canonical な書き方は
  `feFlood` + `feComposite` の**多段フィルタ**になるが、このリポジトリには
  「実機で効くのは1段・`type=linear` だけ。2段に繋いだ版も `feFuncA` を table に
  した版も効かなかった」という記録がある。**いま効いているアルファ抜きを壊す
  危険のほうが大きい。**
- `docs/archive/handoff_v4.md` が既に予告していた——「効かなければ着色は諦めて
  `SAT_TINTS` から出さないことを勧める。iOSのSVGフィルタは**ヘッドレスで再現しない
  不具合を3回踏んでいる**ので、これ以上の深追いは費用対効果が悪い」。そのとおりになった。

### 消したもの

`SAT_TINTS` / `SAT_TINT_DEFAULT` / `satTintDef` / `setSatTint` /
`MAP_LS_SAT_TINT` / `mapPrefs.satTint` / バンドの `tintable` / 着色チップ。
`buildSatFilter(tint, cut)` は `buildSatFilter(cut)` になり、**RGBは常に素通し**。

⚠ **出せない色を選ばせるUIは嘘。** 動かない機能を残すくらいなら消すほうがよい。
⚠ `localStorage` の `sotoki.map.satTint` は**読み書きしなくなっただけ**で、
  既に保存されている端末には残る。読まないので無害。

### 作り直さないための釘

`smoke_mapui` に、**着色が復活していないこと**を見る検査を足した
（チップが無い／`setSatTint` が無い／設定を保存しない／RGBが常に素通し）。
`setSatTint` を復活させる／RGBを定数に差し替える の2通りに壊して落ちることを確認済み。

## 2026-09-20 地図の検査は「演出を止めてから」測る（v4.99.1）

全件実行でときどき落ちる2件の正体が分かった。どちらも**実装ではなく検査の競合**。

| 落ちた検査 | 症状 |
|---|---|
| `smoke_mapscale` 棒の長さと数字が合っている | `20 km(長さ不一致)` |
| `smoke_mapui` 低ズームでは山域名を出さない | 札が 19 枚残る |

原因はどちらも同じ。**Leaflet の `setZoom` / `setView` は既定でアニメーションする**
ので、固定待ち（260ms / 400ms）では `zoomend` / `moveend` に間に合わないことがある。
すると

- メジャーは**前の倍率のまま**・地図は**新しい倍率** → 棒の長さを今の投影で測り直すと合わない
- `drawAreas` がまだ走っておらず、**前のズームの札**が残ったまま数えてしまう

→ `{ animate: false }` を渡して演出を止める。`smoke_mapscale` は固定待ちのままだと
**4回中4回**落ち、直したら4回中4回通った。

⚠ **アプリ側は嘘をついていない。** 棒と数字は `updateMapScale` が**一緒に**書くので、
演出の最中でも「棒と数字」は食い違わない。食い違って見えたのは検査の測り方だけ。

⚠⚠ **`grep '✗'` で落ちたかを判断しない。** 検査ごとに失敗の印が違う
（`smoke_mapscale` は `  - `、多くは `  ✗ `）。実際これで「3回連続PASS」と誤認しかけた。
**`run-all.js` の終了コードと最終行を見ること。**

## 2026-09-20 CORSの失敗は onerror に化ける（v4.100.0）

実機でレーダー実況のストリップも帯も出なかった。**出ないのが仕様どおりで、
その仕様が間違っていた。**

`probeTileAlpha` が例外を投げる道（＝canvasが汚れる）を「読めない」と分類し、
それだけを帯に出していた。ところが `loadTileImage` は

```js
img.crossOrigin = 'anonymous';   // 画素を読むので必須
```

を付けている。**`crossOrigin` を付けた `<img>` は、ACAOヘッダが無いと「汚れる」
のではなく「読み込み自体が失敗」する**（`onerror`）。つまり

- CORSで拒まれる → `onerror` → `kind:'network'` → **黙る**
- `getImageData` が投げる `kind:'device'` → `crossOrigin` を付けている限り**ほぼ到達しない**

**「黙らないため」に作った仕組みが、本番の失敗モードでちょうど黙っていた。**
ADR-0011 と同じ穴を、それを避けるための機能の中で踏んだ。

### 切り分け方

`onerror` からは原因が分からないので、**`mode:'no-cors'` で取り直す**。
不透明な応答でも resolve するので「サーバまで届いたか」だけは分かる。

| no-cors | 意味 | どうする |
|---|---|---|
| 成功 | 届いてはいる＝読めない原因は CORS 側 | `'device'` → **言う** |
| 失敗 | 本当に届いていない | `'network'` → **黙る**（圏外の帯がある） |

⚠ **両方とも言うようにしてはいけない。** 圏外で帯が2本出てチャートが縮む。
⚠ **両方とも黙ってはいけない。** それが今回の不具合。**届くかどうかで分ける。**

### 検査

`smoke_radarcheck` に2つ足した。**届くが画像として読めない**（本番のCORS失敗と同じ形）と
**そもそも届かない**。前者で帯が出ること、後者で黙ることの両方を見る。
v4.98.0 の分類に戻す／届かなくても device 扱いにする の2通りに壊して落ちることを確認済み。

⚠ 実機のJMAが実際にACAOを返すかは**まだ確認できていない**（手元からJMAへ到達できない）。
この修正は「どちらであっても黙らない」ための切り分けであって、CORSだと断定していない。

## 2026-09-20 「stateに入った」と「画面に出た」は別（v4.100.1）

実機で実況のストリップが出ない。**画素で測ったら差分ゼロ＝一度も描かれていなかった。**
`state.radar.ok === true`、13点そろっている、`radarUsable()` も true。**なのに出ない。**

### 原因は私が入れた `redraw: false`

同じ所で2度間違えた。

1. **1回目**: `refreshRadarCheck` が無条件に `buildCharts()` を呼んだ。読み込み直後は
   `applyWeatherJson` の `render()` が rAF で**あとから走る**ので、uPlot を二重に
   組み立ててチャートが壊れた（`smoke_elev` が捕まえた）。
2. **2回目**: それを避けようと初回は `redraw: false` にした。ところが実況の読み取りは
   **タイル13枚ぶんで、必ず `render()` より遅く返る**。つまり
   **結果が出ても誰も描かない**。次の5分後の更新まで永久に出ない状態だった。

→ 旗ではなく**状態**で決める。`chartsReady`（`render()` が `buildCharts()` を
  終えたら立つ）を見る。

| 実況が返るタイミング | `chartsReady` | どうなるか |
|---|---|---|
| `render()` より**早い** | false | 何もしない。これから走る `render()` が描く |
| `render()` より**遅い** | true | ここで描き直す |

どちらでも二重組み立てにならず、**どちらでも必ず描かれる**。

### ⚠⚠ 検査が「state」しか見ていなかった

`smoke_radarcheck` は `state.radar` と帯の文言しか見ておらず、**ストリップが
描かれているかを一度も見ていなかった**。だから3版（v4.98.0 / v4.99.x / v4.100.0）に
わたって緑のまま出荷され、実機で指摘されるまで分からなかった。

→ **実況を消す前と後の画面を突き合わせて、画素で測る**検査を足した。

⚠ 最初「差分の大きさ」だけで書いたら、**縁を外す壊し方がすり抜けた**。
検査の背が濃い青なので、10%の白でも差が出てしまう。
→ **明暗の両方向**を数える（塗り＝明るくなる／縁＝暗くなる）。塗りだけなら
  片方向しか出ないので捕まる。

### 明るい背に埋もれない描き方

雲パネルの背は青にも白にもなる。薄い白だけで描くと**明るい所で消える**。
濃い縁を先に敷いてから塗る。「実況」の札も同じ理由で濃い座布団を敷く。
⚠ **描いてある区間＝見に行けた区間**という読み方の土台なので、
「見えないことがある」は「無い」と区別がつかず致命的。

### 追記: その検査を3回書き直した（v4.100.1）

本体の不具合（実況が一度も描かれない）は一発で直ったが、**それを守る検査**のほうを
3回書き直した。どれも「名前どおりのものを見ていない」という同じ病気だった。

| | 書き方 | 何を見落としたか |
|---|---|---|
| **1回目** | `state.radar` と帯の文言だけ | **描かれているかを見ていない。** 3版にわたって緑のまま出荷した |
| **2回目** | 背景と比べて明暗を数える | **昼夜の陰影で背の明るさが変わる。** 実行した時刻で通ったり落ちたりした（CIで落ちて発覚） |
| **3回目** | 描いた画素の絶対的な明るさ | **「実況」の札が濃いので、縁を外しても暗い画素が残る。** 縁の検査が素通りした |
| **4回目** | 札を除き、**右端の数列だけ**を見る／昼夜の**両方**で測る | — |

⚠⚠ **わざと壊して落ちることを、必ず確かめること。** 2回目・3回目はどちらも
「壊し方を試したら通ってしまった」ことで気づいた。検査を書いた直後に緑なのは、
**正しいから**とは限らない。**見ていないだけ**かもしれない。

## 2026-09-20 切り分けは「いちばん手前で落ちる所」に置く（v4.101.0）

v4.100.0 で `tileReachable`（no-cors で取り直して「届いたか」を見る）を足したのに、
**実機では相変わらず帯も棒も出なかった**。

理由：**その切り分けが一度も動いていなかった。**
`readNowcastSeriesRaw` は、タイルを1枚読む前に `jmaTimesList`（時刻表のJSON）を
`fetch` する。`fetch` は ACAOヘッダが無いと失敗するので、**CORSで弾かれると
タイルより手前でここに来る**。そこは一律 `kind:'network'`＝黙る、のままだった。

→ 時刻表の失敗にも `tileReachable` を通す。届く→`device`（言う）、届かない→
  `network`（黙る）。

⚠⚠ **「切り分けを足した」だけでは足りない。その経路を通るかを確かめること。**
後ろの段だけ直しても、手前で落ちていれば何も変わらない。

### 帯に理由を書く

どの段で落ちたかを画面に出す（`時刻表を読めません` / `タイルの画素を読めません`）。
⚠ ここを省いたせいで、**実機で「出ない」と言われるたびに推測で直す**ことになった
（4往復した）。**画面から原因が読めれば1往復で済む。**

### 検査の作り方の落とし穴

「届くが読めない」を作るのに `route.abort()` を使ってはいけない。
**no-cors の取り直しも落ちるので、それは「本当に届かない」＝黙ってよい側**になる。
→ **中身を壊す**（JSONとして読めない body を返す）。サーバには届くが使えない、
  という CORS と同じ形になる。

## 2026-09-25 実物の時刻表には未来が入っていなかった（v4.102.0）

実機の帯が原因を教えてくれた。

```
⚠ レーダー実況を読めません（時刻表が足りません）
```

**CORSは無関係だった。** 時刻表のJSONは取れているし、パースもできている。
`nowcastSeries` の結果が**1点しか無い**ので、私が置いた `seq.length < 2` で
弾かれていた。つまり**実物の `targetTimes_N1.json` には未来の時刻が入っていない**
（実況だけ）。

### ⚠⚠ 検査のスタブが、実際には無いデータを作っていた

`smoke_rain` / `smoke_radarcheck` の `nowcastTimes()` は
「実況＋5分先×12」を組み立てていた。**そういう形だと思い込んで書いた**もので、
実物と突き合わせたことは一度も無い。だから

- 検査はずっと緑
- 実機では**必ず**弾かれる

という状態が、`rainOutlookNowcast`（v4.67.0〜）から**ずっと**続いていた。
「あと◯分で雨が止みます」が一度も出なかったのはこれが理由（issue #3）。

⚠ **スタブは仕様ではない。** 相手が返す形を確かめずに書いたスタブは、
自分の思い込みを検査で追認するだけになる。

### 直し方

`seq.length >= 2` は私が勝手に決めた条件だった。この機能の本体は
**「いま降っているか」**なので、**実況の1点で成り立つ**。先の時間はあれば描く。

⚠ 「いま」の判定は `radarNowWet()` を使う。**`radarWetAt(new Date())` ではない。**
実況の時刻はたいてい数分前なので、1点しか無いと**範囲外**に落ちて null になる。
古さは `radarUsable()`（20分）が見ているので、直近の1点を「いま」としてよい。
⚠ **`radarWetAt` の窓を広げて解決してはいけない。** あちらはストリップ＝
「どこを見に行けたか」を描くためのもので、広げると嘘になる。

⚠ この検査もまた時刻依存で書きかけた。既定の実況時刻は直近の5分境界なので、
いまが境界の直後だと `radarWetAt(now)` でも範囲内に入り、壊し方がすり抜ける。
`ageMin: 6` で「6〜11分前」に固定して決定的にした。

## 2026-09-26 地図の検索の履歴（v4.103.0）

- **語ではなく選んだ地点を残す。** 語だと押すたびに Nominatim／地理院を叩き直す（圏外寄りの山で待たされる）。
  地点なら座標へ直接飛べ、同名峰の取り違えも起きない
- 上限は100件（利用者の希望）。1件100バイト弱で約10KB。localStorage の上限（おおむね5MB）に対して無視できる
- 100件を一覧だけで出すと長いので、**打つと絞り込む**
- ⚠ `#map-results` が右上の浮きボタンの下に潜っていた（同じ z-index 1100、DOMで後ろのボタンが上）。
  検索結果でも起きていたが、行の右端に✕を置いて初めて押せないと分かった。1150 に上げた
- 検査は `tests/smoke_searchhist.js`

## 2026-09-26 地図の検索が重い（v4.104.0）

- 実機で「やたら重く、検証もできない」。**全部を1本ずつ直列に待っていた**（OSM×3 → 地理院×3）のと、
  **タイムアウトが無かった**のが原因と見た（⚠ 実機での計測はしていない。開発環境からは両方とも到達できない）
- OSMと地理院を同時に回す。**Nominatim の中は直列のまま**（利用条件「1秒1回まで」）。地理院の中も直列にとどめた
  （地理院側の利用条件は未確認なので、並べて叩く数を増やさない）
- 1本5秒で打ち切る。検査では同条件で 1,832ms → 1,400ms 未満
- 却下：届いた側から先に一覧を出す案。行が後から入れ替わると押し間違える（「勝手に並べ替えない」とぶつかる）
- 検査は `tests/smoke_searchspeed.js`
